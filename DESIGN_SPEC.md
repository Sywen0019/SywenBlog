# Sywen’s Space｜视觉设计规范 v1.1

更新日期：2026-09-16（分类同步）。用户批准的 B/E 作业基线优先；v1 历史由 Git 保留。

## 1. 定位与范围

minimal manga / comic / line art / editorial / paper。全站 70% minimal + 30% manga，Hero 可 60/40，表示注意力比例。视觉完成度≈一致性>工程简洁>原预算。保持清爽、中文阅读，不做商业海报、SaaS、赛博朋克或卡片墙。

基线必须：书桌 Hero、统一排版与纸面语言、七页可读、浅深与手机。About 新图、学业／生活／我喜欢的三类小画、植物库、状态变体是 E 类，不阻止 B06。

2026-09-16 场景修订：作者为计算机专业学生；Hero 以笔记本电脑为主要工作设备，书写使用平板与手写笔。头部、眼睛朝向书写位置，禁止一边写一边看镜头的摆拍姿态。

2026-09-16 身份/画法纠正：以三视图为准，眼镜是红色**下半框**，镜片上缘没有红框；不得只写“红色矩形眼镜”。插画使用轮廓线与少量疏排线，设备、键帽、平板边框与笔杆内部留白，不用灰面、涂抹、渐变或色块塑形。眼镜红线是插画的颜色例外；页面CSS纸色/淡蓝不等于允许给插画物件上色。

## 2. 参考与角色

参考素材/五张 PNG 原样保留，不发布。角色三视图唯一身份依据：短发、呆毛、红框眼镜、格纹衬衫。允许新动作和表情，保持五官/头身/衣着一致；不得拉伸、镜像或擅改配色。网页参考用于构图/线稿；背景参考只提供留白与边缘组织。

复杂场景、人物与重绘由 Astra 在 Codex 调用内置出图；简单无人物素材由豆包外部执行。基线只生成 A01 Hero，两候选后择一，局部迭代。提示词、工具、源图、版本与选稿理由见 docs/art-direction.md；不宣称未经工具确认的生成模型版本。

## 3. 保留的视觉基础

保留 css/base.css 已有语义色、字体、1px 分隔/2px 控件线、3/4px 硬阴影、4px 控件圆角；暖纸、墨色、淡蓝、少量便签黄，红色主要属于角色。原生系统中文字体，品牌 Georgia，代码 Consolas 等本地栈；不请求外部字体。

内容最大 1120px、Post 740px。正文手机17px/桌面18px、行高1.85；间距沿用4/8/12/16/24/32/48/64。断点768/1024，标题自然换行，无固定首屏高度。一般正文对比≥4.5:1、大字和关键非文本≥3:1。

## 4. 逐页完成度

- Home：Hero → 最近三篇 → 三类入口（学业／生活／我喜欢的）→ 三格近况。Hero 桌面约等宽两列，4:3书桌场景约440–520px；手机文案和CTA后显示。标题、品牌、旁白均为HTML真实文字。Hero使用空alt，邻近文本承担介绍。
- Blog：标题→搜索→分类→结果工具→单列条目/无结果。基线条目必须日期、分类、标题、摘要、阅读时间；E01才加入小画，桌面128×96、手机80×60。
- About：连续介绍文本块与现有正面图排两列，窄屏单列；学习/兴趣/近况/静态FAQ；图alt准确描述漫画形象，不编造履历。
- Post：阅读列740px，正文无背景纹理或人物；代码局部滚动，引用/表格/图注/相邻链接完整。长内容自然换行。
- 未完成主题/筛选以外增强控件保持hidden；菜单和阅读工具留E类。

## 5. 装饰与深色

每页一个主要焦点，每section最多一组1–3个相关元素；Hero最多两组辅助。手机保留一处身份细节，不一律全删。装饰aria-hidden、不可聚焦、pointer-events:none，不覆盖正文或焦点。胶带、网点、分镜、简单箭头用CSS/SVG；有笔触人物/物件用图像，不把页面烘焙成图。

深色保留Hero的暖纸色画内背景，外部使用深色Tokens；不反色人物、不整体降低透明度。原两张裁切可保留白纸面，避免扩大无意义白框。

## 6. 图像导出与降级

Hero WebP单个实际请求≤300KB；About≤200KB；后续分类每张≤40KB、装饰≤25KB、状态≤40KB。首页初始≤800KB、滚动全部≤1.2MB是目标；超出先优化编码/尺寸/裁切再记录影响，不默认删主视觉。

Hero不懒加载，使用srcset/sizes、width/height与固定比例占位；其余按位置加载。母版候选不上线。生成成功不等于验收通过。图片失败保留空间，图框显示Sywen；有脚本时隐藏破图，无脚本仍有旁白和导航。

## 7. 验收

VC0：Hero1440/390浅深对照三视图，角色、书桌叙事、文本入口、手机清晰度。
VC1-B：实际查看四类页面，320/390/768/1440及断点、200%缩放、键盘、无脚本/存储失败/图片失败、子目录。基础风格一致，正文不受干扰。未执行设备记录未验证。
VC2：只检查实际新增E类效果和功能，允许基线保持不变。不能以未生成E类资产判基线失败。

## 8. v1限制变更表

|旧要求|v1.1替代|
|---|---|
|80/20、仅裁切、禁止动作|70/30，Hero60/40；允许身份一致的新动作|
|只两张角色图、360px高度硬限|基线一张书桌主图；About复用；Hero按4:3文图关系|
|Blog禁止装饰、手机全部隐藏|区域密度控制；E类小画；手机保留身份细节|
|首页500KB|初始800KB、全量1.2MB目标|
|全部交互/完整资产库首发|主题与筛选进入B；菜单/阅读/完整资产库进入E|
