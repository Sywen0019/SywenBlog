# Sywen's Space｜视觉设计规范

版本：1.0 · 编制日期：2026-09-15  
状态：视觉规范已编制；图片派生、网页实现及浏览器验收尚未执行。  
适用：原生 HTML / CSS / JavaScript 多页面个人博客。

本规范把现有参考素材转化为可实现的视觉规则。实现时结合 [项目规划](PROJECT_PLAN.html) 使用：本文规定视觉，项目规划规定内容、交互及交付范围。本文中的派生图片路径、组件和页面均是后续实现契约，不代表文件已存在。

## 1. 定位、优先级与规划差异

### 1.1 设计定位

**简约、有趣、年轻的二次元漫画线稿风个人博客。**

关键词：manga / comic / line art / editorial / paper / sketch / minimal / anime-inspired。

以中文内容、清晰层级和留白形成 80% 的视觉骨架，以人物线稿、纸面分镜、少量旁白形成 20% 的趣味。这个比例是注意力与装饰预算原则，不要求按像素面积计算。

- 读者首先能辨认作者、文章标题和阅读入口，其次注意到人物与装饰。
- 界面、提示和正文为中文；保留英文品牌 Sywen's Space，副标题为“代码 · 科研 · 生活”。
- 不转向 Liquid Glass、SaaS、Dashboard、Cyberpunk 或商业官网风格。
- 不使用大面积渐变、玻璃模糊、发光边框、浮动卡片墙和连续角色动画。

### 1.2 决策优先级

本次已确认的视觉方向与角色保护要求 > 项目规划中的页面结构、功能与阅读约束 > 参考图具体表现手法。本文已经解决的视觉差异按下表执行，不要求实现者重新选风格。

| 原规划或参考内容 | 本次决定 | 保留的约束 |
|---|---|---|
| 原规划 §7、§10、§21、§25、§28 的抽象书桌主插画及 hero-desk.svg | 主视觉改用现有角色正面裁切；不再要求同时制作书桌主插画 | 首屏一处主线稿，内容优先 |
| 参考图四列带封面的最近文章 | 改为日期窄列＋内容宽列的三篇条目 | 原规划 §10 的最近三篇与单列阅读路径 |
| 参考图英文导航与文案 | 使用项目规划的中文导航、标题和提示 | 品牌保留英文 |
| 参考图常驻右侧菜单、Theme 子菜单箭头 | 使用临时单层菜单，无子菜单箭头 | 原规划 §15 的动作和键盘行为 |
| 背景图包含多个角色和密集边缘插画 | 提炼留白与纸感，不直接铺整图 | 正文零背景装饰 |
| 辅助参考中的收藏、封面墙、附加配色 | 不引入首发 | 不扩大 P0 功能范围 |
| 原规划“当前目录为空”、AGENTS.md“四张参考图” | 调查时实际为规划文件、仓库说明与五张 PNG | 历史描述不作为当前文件事实 |

404 只在本文预定义视觉，仍属 P1；首发仍为 Home、Blog、About 和四篇 Post。文章目录、收藏及其他增强功能不因本文自动进入首发。

## 2. 视觉资产清单与参考分析

### 2.1 原始资产登记

全部原图均为 RGB PNG，无透明通道。MB 按十进制显示；字节数为调查值。原文件保留在 `参考素材/`，不覆盖、不移动、不在网页中直接加载。

| ID | 原文件 | 尺寸 | 字节 / 约 MB | 优先级 | 用途及上线结论 |
|---|---|---|---|---|---|
| A01 | [角色三视图.png](参考素材/角色三视图.png) | 1448×1086 | 1,546,263 / 1.55 | 一级 | 角色身份依据；仅其正面裁切作为网页派生图片 |
| A02 | [背景参考图.png](参考素材/背景参考图.png) | 1672×941 | 1,768,001 / 1.77 | 二级 | 中央留白、纸感、边缘组织参考；整图不上线 |
| A03 | [网页参考图.png](参考素材/网页参考图.png) | 1672×941 | 1,865,231 / 1.87 | 一级 | Hero 文图关系、导航和菜单气质参考；截图不上线 |
| A04 | [网页辅助参考1.png](参考素材/网页辅助参考1.png) | 1672×941 | 2,109,996 / 2.11 | 三级 | 胶带、旁白、线框按钮和选中状态参考；截图不上线 |
| A05 | [网页辅助参考2.png](参考素材/网页辅助参考2.png) | 1672×941 | 2,056,424 / 2.06 | 二级 | 留白、标题层级、分区节奏参考；截图不上线 |

来源记录为“用户提供的工作区参考素材”；不擅自补写作者、生成工具或授权结论。后续 README 如登记来源，沿用这一可核实描述。

### 2.2 参考特征转译

| 观察 | 提炼的规则 | 应用位置 | 不采用的部分 |
|---|---|---|---|
| A01 正、侧、背视图，短发、呆毛、红眼镜、格纹衬衫、长裤、运动鞋 | 身份以 A01 为准；保留线稿与局部红色 | Hero、About、空状态和 404 | 三视图整张展示、姿势重绘、自动描摹 |
| A02 中央留白、两侧人物与植物 | 阅读区完整留白，趣味限定在局部图框 | 页面纸底与 Hero 插图区 | 全屏 cover 背景、固定两侧人物、植物满屏 |
| A03 左文右图、刊头、细线分区、淡蓝强调 | 文字先行，采用 60:40 文图区；边框按层级分粗细 | Home、导航、菜单 | 四列卡片、灰色小字过多、菜单永久占位 |
| A04 手绘线框、胶带、网点、蓝色选中项 | 用 CSS 小装饰及统一选中背景建立漫画感 | Hero 图框、按钮、分类 | 多个人物并列、全文手写字体、粉蓝类别泛滥 |
| A05 大留白、轻分隔、清晰标题 | 通过垂直节奏分区，不依赖容器卡片 | 各页标题、文章条目 | 过大的桌面人物、收藏功能、绿色新主题 |

色值采用项目规划的纸色体系，参考图用于判断关系与密度；本文色值不是声称从图片精确取样得到的结果。

## 3. 图片使用、裁切与导出

### 3.1 派生文件契约

只制作两种裁切，复用于不同页面；不为每个页面另画角色。坐标以 A01 原图左上角为原点，单位 px，裁切矩形为 `(x, y, width, height)`。

| 派生名称（相对站点根目录） | 来源与裁切 | 导出尺寸 | 使用与显示上限 | 体积目标 |
|---|---|---|---|---|
| assets/images/character-front-upper.webp | A01 `(120, 0, 320, 600)`；头顶至上腿的正面裁切，保持双臂轮廓 | 320×600，不放大 | Hero、About、404；高度分别最高 360、300、240px | ≤100KB |
| assets/images/character-front-avatar.webp | A01 `(175, 0, 225, 225)`；头顶、脸及肩颈 | 225×225，不放大 | Empty State；桌面/平板 80px，手机 64px | ≤30KB |

上述裁切是基于已查看原图确定的导出窗口。制作时核验头顶呆毛、红眼镜及轮廓完整，窗口内无相邻人物或英文标签；若发现边缘需要调整，只允许向原图空白区微调，并把最终坐标同步回本文。不得为凑比例拉伸人物或补画缺失部位。

- 图片保持原比例；上身图宽度由高度与 320:600 比例决定，桌面 Hero 最高 360px 时约宽 192px。40% 指插图区列宽，不是强行把人物拉满该列。
- 保留原有浅底与线条，不抠图、不滤镜反色、不整体降低角色透明度。
- WebP 初始导出质量 90，检查红眼镜、发丝和格纹；只在细节仍清楚时降低质量。超过预算时先去除无用元数据、优化编码，不删改角色线条。
- 保留源分辨率，不能通过上采样声称提供更清晰的 2× 图片。在实际显示尺寸及高像素密度设备上验收。
- 不生成额外 PNG 回退副本；目标浏览器为原规划中的 Chrome、Edge、Firefox 和手机现代浏览器。
- 设置固有宽高或等价宽高比，图像加载期间保留布局空间；Hero 图正常加载，About 图与状态图按实际出现位置加载。
- 同页同一区域只显示一个角色。资源复用同一地址，避免为不同页面复制相同图片。

### 3.2 替代文本与失效策略

- Hero 中与作者介绍重复的角色图、404 和 Empty State 中纯辅助角色图使用空替代文本；图旁的标题、说明和操作均为真实文本。
- About 角色图承担作者视觉形象展示，替代文本为“Sywen 的漫画角色形象：短发、红框眼镜和格纹衬衫”。不将它描述为真人照片。
- 图片加载失败时保留既定空间并隐藏破损图标，角色图框显示短文本“Sywen”；空状态小图可直接隐藏。旁边的标题和操作始终可用。
- 真实文章图片根据内容编写替代文本与图注，不套用角色规则。

### 3.3 图片、CSS、SVG 分工

| 内容 | 实现介质 | 约束 |
|---|---|---|
| 人物、真实文章插图 | 本地图片 | 不把参考页面截图作为网页主体 |
| 纸底、留白、边框、阴影、状态底色 | CSS | 不以图片替代可适配布局 |
| 胶带、稀疏网点、强调下划线 | CSS 伪元素或独立装饰元素 | 无指针事件，不影响文本和焦点 |
| 首页、文章、搜索、主题、复制等图标 | 简单内联 SVG | 24×24 视框、2px 描边、统一圆线帽和圆连接，无图标库 |
| 正文、导航、按钮、旁白 | HTML 文本 | 不烘焙进图片；图标不能替代动作名称 |

## 4. Color System 与浅深主题

页面依靠纸色而不是纹理建立纸感。蓝色只作为局部背景，黄色只用于少量提示；人物眼镜保留原红色，不扩展成界面强调色。

| 语义 Token | 浅色 | 深色 | 使用及限制 |
|---|---|---|---|
| `--color-paper` | `#F7F3EA` | `#202223` | 全页背景、正文底色 |
| `--color-surface` | `#FFFCF5` | `#292C2E` | 菜单、输入框、代码块、旁白面板 |
| `--color-ink` | `#242424` | `#F2EEE5` | 主文字、交互边框、图标 |
| `--color-muted` | `#625F59` | `#BBB6AC` | 日期、图注、辅助文字，不再用透明度淡化 |
| `--color-rule` | `#D6CFC2` | `#494D4F` | 装饰分隔线，不承担输入框边界或焦点 |
| `--color-accent` | `#D9E8EC` | `#344B53` | 选中和悬停背景，其中文字为 ink |
| `--color-note` | `#F0E1AC` | `#50482E` | 示例标识、需注意的短提示，其中文字为 ink |
| `--color-shadow` | `#242424` | `#111314` | 硬阴影；深色轮廓由 ink 边框保障 |
| `--color-art-paper` | `#FFFFFF` | `#FFFFFF` | 仅原图纸面，深色下仍保留，不用于普通控件 |
| `--color-focus` | `var(--color-ink)` | `var(--color-ink)` | 键盘焦点；不使用淡蓝线代替 |
| `--color-link` | `var(--color-ink)` | `var(--color-ink)` | 正文链接，始终有下划线 |
| `--color-selection` | `var(--color-note)` | `var(--color-note)` | 文本选区，前景为 ink |

浅深主题覆盖全部交互状态、提示面板与代码块。深色下的白底角色图是明确的插画纸面例外，不是遗漏主题映射；不叠加大面积白色外框或白底容器。

正文及辅助文字至少 4.5:1，大字和关键非文本控件至少 3:1。实现后按实际背景组合测量；弱分隔线不作为唯一交互提示。成功和失败通过明确文案、图标和操作表达，不新建大面积红绿状态色。

主题初始顺序延续原规划：合法保存值 → 系统主题 → 浅色。视觉不能依赖脚本加载后再反转整页颜色；Header 与菜单的主题动作名称保持同步。

## 5. Typography

### 5.1 字体

| Token | 完整字体栈 | 用途 |
|---|---|---|
| `--font-ui` | `-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif` | 中文正文、标题、按钮、旁白 |
| `--font-brand` | `Georgia, "Times New Roman", serif` | 英文品牌刊头，不用于中文正文 |
| `--font-code` | `Consolas, "SFMono-Regular", "Liberation Mono", monospace` | 行内代码、代码块 |

不加载远程字体，不假设某款手写字体已安装。漫画趣味来自分镜和小装饰，不能依赖全文手写字体。正文不使用字间拉伸；品牌可用斜体，导航、菜单和正文保持正体。

### 5.2 字号与排版

| Token | 值 / 响应式变化 | 行高 | 字重与使用 |
|---|---|---|---|
| `--text-hero` | `clamp(34px, calc(7.222px + 8.368vw), 60px)` | 1.2 | 700；320px 为 34px，约 631px 起达上限，允许自然换行 |
| `--text-page-title` | `clamp(30px, calc(24.545px + 1.705vw), 42px)` | 1.3 | 700；320px 至 1024px 流式增长 |
| `--text-h2` | Mobile 24px；Tablet/Desktop 28px | 1.4 | 700 |
| `--text-h3` | 20px | 1.5 | 700 |
| `--text-entry-title` | Mobile 20px；Tablet/Desktop 22px | 1.5 | 700；文章条目标题 |
| `--text-ui` | 16px | 1.65 | 400；控件标签 600 |
| `--text-body` | Mobile 17px；Tablet/Desktop 18px | 1.85 | 400；文章正文 |
| `--text-meta` | 14px | 1.6 | 400；日期、分类、图注 |
| `--text-code` | Mobile 14px；Tablet/Desktop 15px | 1.7 | 400；行内代码按所在正文的 0.9em |
| `--text-brand` | Mobile 24px；Tablet/Desktop 28px | 1.3 | 700 |

标题不做强制单行、不通过缩字挤进固定高度。长英文词和网址允许断行；代码块保留原始空白并局部滚动。正文段落间距 24px；h2 前后为 48/16px，h3 前后为 32/12px；列表项间距 8px。标题紧接区块开头时取消多余前距。

## 6. Layout 与 Spacing

### 6.1 容器与网格

- 公共内容容器最大 1120px，水平居中。这里的 1120px 是内容宽度上限，不包含两侧视口留白。
- 可用宽度为视口宽度减两侧 gutter；Desktop/Tablet/Mobile gutter 分别为 32/24/16px。
- Post 正文及文章头部最大 740px；宽屏保持留白，不添加填充侧栏。
- Header/Footer 使用公共容器；Header 正常文档流，不固定。
- Hero Desktop 为扣除 gap 后的 3:2 两列，gap 48px；Tablet 为 3:2，gap 24px；Mobile 为单列，gap 24px。
- 不使用固定首屏高度或 100vh 限制内容；标题换行和浏览器缩放可以自然增加高度。
- 文章条目 Desktop 为 112px 日期列＋内容列，gap 24px；Tablet 日期列 88px；Mobile 日期成为内容上方元信息。

### 6.2 垂直节奏

| 区域 | Desktop | Tablet | Mobile |
|---|---|---|---|
| Header 上下 padding | 24px | 24px | 16px |
| Header 内部 gap | 24px | 16px | 12px |
| 主内容顶部 / 底部留白 | 48 / 64px | 32 / 48px | 32 / 48px |
| 首页大区块间距 | 64px | 48px | 48px |
| Hero 标题至简介 | 24px | 24px | 16px |
| 简介至 CTA | 24px | 24px | 24px |
| CTA 之间 | 12px | 12px | 12px，可换行 |
| 标题至区块内容 | 24px | 24px | 16px |
| 文章条目上下 padding | 24px | 24px | 24px |
| 面板内边距 | 24px | 24px | 16px |
| Footer 顶部留白 | 32px | 32px | 32px |

基础间距仅使用 4、8、12、16、24、32、48、64px；图像固有比例和视口定位计算不受此步长限制。

## 7. Border / Line、Shadow、Radius

- 分隔线 1px，颜色为 rule；按钮、输入框、菜单和主分镜轮廓 2px，颜色为 ink。
- 功能图标统一 2px 描边。小尺寸图标按 24×24 视框缩放到 20px，不混用填充与线性风格。
- 默认无阴影；按钮用向右下 3px 的硬阴影，主 CTA 和菜单为 4px；模糊半径、扩散半径均为 0。
- 正文、文章条目、整页布局不加硬阴影；避免全部元素同时成为漫画框。
- 页面和图片裁切默认直角；标签 2px；按钮、输入框、菜单及提示面板 4px。
- 头像采用方形裁切，不额外圆形遮罩；主题切换使用普通线框动作按钮，不沿用参考中的大胶囊开关。
- 文字下划线 1px，偏移 4px；Hover 可加粗至 2px。当前导航同时使用字重 700 与 2px 下划线。

## 8. Manga Decoration 装饰预算

允许：纸面分镜、短旁白、单片胶带、小片网点。每个装饰必须属于明确容器，不侵入文章内容。

| 装饰 | 几何与密度 | 默认位置 | 预算 |
|---|---|---|---|
| 胶带 | 48×16px，accent 底色，最多旋转 -1° | Hero 角色图框右上，位于插图区内 | 1 处 |
| 网点 | 最大 64×64px；1px 圆点、8px 间隔，ink 色、opacity 0.08 | Hero 图框右下附近，不覆盖人物和文字 | 1 处 |
| 旁白 | 14px 正体文字，最多两行；“学习中，持续更新。” | Hero 角色图下，间距 12px | 属于主图说明，不再追加气泡装饰 |
| 线框 | 2px ink，0 圆角 | 角色图框 | 主插画的一部分 |

首页首屏最多一处角色主图＋两处小装饰。背景和前景合并计数，不因使用伪元素而豁免。About 最多一片胶带，无网点；Blog、Post、404、Empty State 不额外添加装饰。

移动端移除胶带、网点和其他非必要装饰；保留人物与文字说明。装饰隐藏于辅助技术、不可聚焦、不可响应指针，不能挡住焦点环。禁止植物边框满屏、速度线、漂浮花瓣、贴纸堆叠和鼠标跟随。

## 9. 角色使用规范

### 9.1 角色保护

A01 是唯一角色身份依据。保留原始短发轮廓、呆毛、红色眼镜、五官、格纹和服装比例。其他参考图中的眼镜形状、姿势和服装差异不作为新增角色变体。不得改表情、增加动作、重绘手部、修改服装、添加身体部件、镜像翻转或改变配色。

### 9.2 位置与尺寸

| 页面 / 状态 | 派生资产 | Desktop | Tablet | Mobile | 文图关系 |
|---|---|---|---|---|---|
| Home Hero | front-upper | 图高≤360px | 图高≤300px | 图高≤220px | 双栏时居插图区中央；单栏时在文案和 CTA 后 |
| About | front-upper | 图高≤300px | 图高≤240px | 图高≤220px | 桌面介绍左、图右；窄屏先介绍再图 |
| 404，P1 | front-upper | 图高≤240px | 图高≤240px | 图高≤180px | 标题和恢复入口优先，图在说明后 |
| Blog 无结果 | front-avatar | 80×80px | 80×80px | 64×64px | 标题、说明、重置按钮在先，头像在后 |
| Blog 正常列表、Post | 无 | — | — | — | 不添加侧边人物 |

尺寸约束同时受容器宽度限制，图片使用 contain 等价规则，不裁切显示内容；图框在上述图片外增加 8px surface 内边距与 2px ink 边框。About、404 复用同一文件，由显示尺寸控制；不创建独立姿态版本。

深色下保持相同尺寸及色彩，浅色仅出现在角色自身纸面。不能给整个插图区铺白底。角色不固定、不悬浮、不随滚动放大；不得挤压正文列来满足人物尺寸。

## 10. 背景分层与逐页降噪

固定绘制顺序：页面 paper → 局部 surface → 局部装饰 → 内容与控件。正文背景不使用 opacity 降噪；从源头不放装饰。

| 页面 | 页面底层 | 局部表现 | 不允许 |
|---|---|---|---|
| Home | 纯 paper | Hero 图框附近按 §8 使用胶带和网点 | 整张 A02 铺底、正文下面叠人物 |
| Blog | 纯 paper | 搜索框 surface，选中分类 accent | 列表纹理、条目背景人物 |
| Post | 纯 paper | 代码块 surface、引用左边线 | 正文网点、纹理、固定边缘插画 |
| About | 纯 paper | 一处角色分镜和胶带 | 两侧站立人物、重复角色 |
| 404 / Empty State | 纯 paper | 小角色图与真实文字 | 背景场景或额外网点 |

不使用全屏背景图片、固定背景、视差或背景动画。A02 只作为留白和纸感依据，不生成独立网页背景文件。深色主题不通过反色滤镜处理整页；Mobile 的背景降噪由隐藏小装饰完成。

## 11. 页面视觉规范

### 11.1 Home

顺序：Header → Hero → 最近文章 → 分类入口 → 最近在做 → Footer。

- Hero 标题“你好，我是 Sywen！”，简介沿用项目规划。以主 CTA“阅读文章”和次 CTA“关于我”引导访问，按钮不得埋在人物下方。
- 最近文章显示日期倒序前三篇，条目内容为分类、日期、标题、摘要与阅读入口。条目标题和阅读入口为真实链接，摘要完整展示，不用行数截断隐藏信息。
- 最近文章区标题行右侧提供“查看全部文章”；窄屏可移到下一行，不缩小文字。
- 分类为“人工智能、编程、科研、生活”四个真实入口，展示实际数量。横向排列、自然换行；不使用封面卡片。
- “最近在做”只用一个旁白面板，三项内容按原规划；Desktop/Tablet 三列、Mobile 三行。每项标签与说明间隔 8px，列间隔 24px。
- 最近文章以下不新增角色、网点或背景图。Header/Footer 线条仅做结构分隔。

### 11.2 Blog

顺序：标题与说明 → 搜索 → 分类 → 结果数量与重置 → 单列文章 → 无结果状态（与结果列表互斥）。

- 搜索输入区最大 560px，Mobile 占满可用宽度；标签位于上方，间距 8px；输入框最小高 44px，内边距 8px 12px，surface 底色。
- 搜索占位提示“搜索标题、摘要或标签”，不能替代可见标签“搜索文章”。
- 分类为“全部 / 人工智能 / 编程 / 科研 / 生活”，分类组与搜索相距 24px，按钮间距 8px。
- 结果工具行距列表 16px，文字 14px；重置操作最小点击高度 44px。窄屏可换行，不横向压缩。
- 条目沿用 Home 的日期列布局；标签换行，标签间距 8px。既有功能仍为搜索与分类组合，不新增分页或列表骨架动画。
- 无结果文案为“没找到匹配的文章。试试其他关键词，或重置筛选。”，配“重置筛选”；状态块上下 padding 32px，无厚边框。头像不可占据操作之前的阅读顺序。
- 输入更新不移动焦点。结果即时可见，延迟播报依原规划执行；不使用列表飞入、渐隐后再更新等效果。

### 11.3 Post

顺序：返回文章列表 → 分类、标题、日期、阅读时间 → 示例标识 → 正文 → 标签 → 相邻文章 → Footer。

- 文章标题使用 page-title，而不是 Hero 字号；元信息自然换行，项间隔 12px；头部至正文 32px。
- 正文宽度≤740px，字号与段落节奏见 §5。只有一个 h1，正文从 h2 开始。
- 引用使用 2px ink 左边线，左 padding 16px，上下间距 24px；可加文字“笔记”，无气泡尾巴和大外框。
- 行内代码采用 surface 底、2px 圆角、2px 4px padding；长片段可换行。代码块采用 1px rule 边框、surface 底，Desktop/Tablet padding 24px、Mobile 16px，独立横向滚动，无语法高亮库。
- 正文表格采用 1px rule 分隔、左对齐、单元格 12px padding；宽表格置于可键盘访问的局部滚动区。
- 正文图片不超过列宽、不拉伸、不加装饰倾斜；图注 14px muted、距图 8px；图片区块上下间距 24px。
- 示例文章标识使用小型 note 背景和真实文字“示例文章”，不把整篇正文染色。
- 标签与正文间隔 32px。相邻文章顶部 1px rule，padding-top 24px，Desktop/Tablet 两列、Mobile 单列，gap 24px。没有相邻文章时省略对应链接，不画不可用按钮。
- 顶部阅读进度为 2px ink 实线，贴视口顶部；按整页滚动进度计算。首发无 TOC 或侧栏。

### 11.4 About

顺序：关于我 → 学习方向 → 兴趣 → 最近在做 → 常见问答。

- 桌面顶部为介绍与角色侧信息两列，比例 3:2，gap 48px；Tablet/Mobile 单列，gap 24px。后续内容按章节流式排列，不建立卡片矩阵。
- 学习方向和兴趣采用文字分组，组间 24px；不使用技能百分比、雷达图或虚构履历。
- “最近在做”复用旁白面板。FAQ 为三组静态问答，问题字重 700，回答普通字重，问答间 8px、组间 24px；不用折叠箭头暗示不存在的交互。
- 未提供的邮箱、学校和社交账号不展示；不创建空链接。

## 12. 组件与状态规范

### 12.1 公共组件

| 组件 | 默认 | Hover / 当前或选中 | Pressed / Focus | 禁止或异常处理 |
|---|---|---|---|---|
| 品牌和导航 | ink，品牌衬线，导航 16px | 当前导航 700＋2px 下划线；Hover 加下划线 | Focus 统一轮廓，不位移 | 普通导航不使用 menu 角色 |
| 主 CTA | accent、2px ink、4px 阴影、4px 圆角 | 保持 accent，下划线或阴影反馈 | Pressed 位移 2px、阴影降至 2px；Focus 轮廓可叠加 | 不使用大面积蓝色渐变 |
| 次按钮 | surface、2px ink、3px 阴影 | accent 底色 | Pressed 位移 1px、阴影降至 2px | 链接与按钮语义按动作区分 |
| 分类按钮 | paper、2px ink、无阴影 | Hover accent；选中 accent＋700 | Focus 轮廓；选中同步 aria-pressed | 选中不能只靠淡色 |
| 文章条目 | 无卡片背景、底部 1px rule | 标题链接下划线加粗 | 仅链接获得焦点；条目不漂浮 | 不把整行嵌套为多个重叠链接 |
| 输入框 | surface、2px ink、4px 圆角 | 不强制附加 Hover 动画 | Focus 外轮廓，不改变尺寸 | 标签可见，原生选区与右键保留 |
| 标签 | surface、2px 圆角、4px 8px padding | 可点击时下划线反馈 | 可点击标签提供足够点击区与焦点 | 非交互标签不呈现按钮阴影 |
| 旁白面板 | surface、2px ink、4px 圆角、无阴影 | 无 Hover 状态 | 无焦点，除非内部有链接 | 不给纯文本添加可点击外观 |
| 返回顶部 | 44×44px 以上、surface、2px ink | accent 底色 | Focus 轮廓，点击后聚焦主标题 | 隐藏时不占 Tab 顺序 |

按钮水平 padding 16px、垂直 padding 8px，最小高度 44px，标签可换行。功能图标通常 20px，与文字间距 8px；纯图标按钮必须有中文可访问名称。不存在实际禁用条件时不添加禁用按钮；如复用组件确需禁用，用 muted 文本、rule 边框、无阴影与原生禁用语义，不整体降低面板透明度。

Header Mobile 第一行为品牌及主题动作，空间不足工具可换行；第二行完整展示三个导航，gap 24px，不添加汉堡菜单。快捷菜单入口仅在输入能力满足时出现；它不能挤掉普通导航。Footer 可换行，预留至少 64px 底部空间，避免返回顶部控件覆盖最后一行。

返回顶部仅在滚动超过 480px 时显示，距离视口右侧和底部至少 16px，叠加设备安全区；动作依据原规划。浮动控件不侵入正文主列。

### 12.2 自定义右键菜单

菜单外观是小型漫画纸面：surface 底色、2px ink 边框、4px 圆角、4px 硬阴影。宽 224px（包含边框和 padding），最大宽度为视口减两侧各 8px；默认内边距 8px。

内容分组固定为：

1. 首页、文章（Post 中为“返回文章列表”）、关于。
2. 搜索文章、返回顶部、切换至深色／浅色。
3. 复制页面链接（Post 中为“复制文章链接”）。
4. 仅 Post：只读“阅读进度：N%”。

- 分组间 1px rule 线，上下留 8px；操作行 min-height 40px，padding 8px，文字 16px、图标 20px，gap 8px，长文案可换行。
- Hover 为 accent 背景；键盘聚焦项同时有清晰 ink 轮廓，轮廓在菜单可见内边距中完整显示。菜单项 Pressed 不位移、不影响定位。
- 只读进度区为 14px muted，不参与方向键循环，不使用 Hover 底色。
- 不放角色、胶带、网点、旁白脚注和子菜单箭头；不旋转，不加背景遮罩，不保留固定侧栏空位。
- 菜单挂到 body，以 fixed 定位；测量实际尺寸后限制在视口内。四周至少 8px，右侧和底部额外考虑 4px 阴影，防止阴影被裁切。
- 高度不足时内部滚动，焦点项应滚入可见区域；宽度限制同时考虑滚动条。菜单内部滚动不触发自身关闭。

启用条件为 `(hover: hover) and (pointer: fine)`。链接、媒体、输入框、可编辑区、非空文本选区、Shift 右键及显式豁免区域使用原生菜单；检查点击目标及祖先。触摸长按不拦截。

通过可见“快捷菜单”按钮以 Enter/Space 打开，聚焦首个可执行项；↑/↓ 循环、Home/End 到首尾、Enter/Space 执行。Esc 关闭并恢复焦点；Tab/Shift+Tab 关闭并进入正常焦点顺序。外部点击不抢回焦点；搜索和返回顶部以动作目标为最终焦点。

执行动作、外部点击、Esc、页面滚动、Resize、窗口失焦或输入能力变化时关闭。再次右键更新同一个菜单的位置；隐藏菜单不可聚焦。主题动作与 Header 共用状态；搜索与复制复用已有公共动作，不增加第二套 API。

### 12.3 提示与复制降级

- 复制成功显示“链接已复制”。全局状态区使用 surface、1px ink 边框、4px 圆角、12px 16px padding、14px 文字，无动画位移。采用状态播报，不弹 alert，不移动焦点。
- 提示最大宽度 360px，位于底部居中、距边缘至少 16px。页面内容保留底部空间；提示无操作且不阻挡指针，约 4 秒后消失。层级使用 floating。
- 复制失败显示非模态手动复制面板，宽度≤420px且≤视口减两侧 16px，居中固定，surface、2px ink、4px 圆角、4px 硬阴影、24px padding；Mobile padding 16px。低高度时面板内部可滚动，轮廓不得裁切。
- 面板包含“无法自动复制，请手动复制链接”、可见标签“页面链接”、只读地址输入框和“关闭”按钮。聚焦并选中输入内容，输入框保留原生菜单；Esc/关闭退出并恢复合理焦点。面板不增加遮罩或强制焦点陷阱。
- file:// 下显示“请在公开网站中复制可分享链接”，不复制本地磁盘路径。失败提示保留至用户关闭，不自动消失。

## 13. 404 与 Empty State

404 为 P1 视觉预案，不代表本期新增路由或承诺托管平台的 404 行为。

- 内容最大宽度 640px，居中但正文左对齐；顶部“404”为 14px 元信息，主标题“这页暂时找不到了”使用 page-title。
- 说明“可以回到首页，或去文章列表继续阅读。”，下方间隔 24px 放“返回首页”和“浏览文章”。角色在说明及操作之后，间距 24px，按 §9 限制尺寸。
- 不使用困惑、哭泣或其他重绘表情，不给角色增加问号道具。页面不足一屏时正常流式排版，不依赖强制垂直居中。

Empty State 属于 Blog P0，沿用 §11.2。没有搜索结果时显示文字、重置动作和小头像，隐藏文章结果条目；重置后立即恢复结果并移除状态块。不会把空结果描述为错误，也不增加重试网络请求按钮。

## 14. Responsive Rules

断点为 Mobile <768px、Tablet 768–1023px、Desktop ≥1024px，采用移动优先的实现顺序。

| 区域 | Mobile | Tablet | Desktop |
|---|---|---|---|
| gutter | 16px | 24px | 32px |
| 公共容器 | 可用宽度 | 可用宽度 | ≤1120px |
| Header | 品牌工具行＋完整导航行 | 单行，必要时换行 | 单行 |
| Hero | 文案、CTA、人物单列 | 3:2 双栏，gap 24px | 3:2 双栏，gap 48px |
| 人物上限 | Hero/About 220px 高 | Hero 300px、About 240px 高 | Hero 360px、About 300px 高 |
| 小装饰 | 全部隐藏 | 使用既定预算 | 使用既定预算 |
| 文章日期 | 标题上方 | 88px 窄列 | 112px 窄列 |
| 搜索 | 占满可用宽度 | ≤560px | ≤560px |
| 分类 | 自动换行 | 自动换行 | 横排，容纳不下时换行 |
| 最近在做 | 三行 | 三列 | 三列 |
| About | 单列 | 单列 | 介绍与角色两列 |
| 正文 | 17px，宽度自适应 | 18px，≤740px | 18px，≤740px |
| 相邻文章 | 单列 | 两列 | 两列 |
| 右键菜单 | 按输入能力判断；触摸关闭 | 按输入能力判断 | 按输入能力判断 |

以内容重排兜底：所有 Grid/Flex 内容列可收缩，长标题/网址可换行；按钮组允许换行。不能用全局 overflow-x hidden 掩盖布局缺陷。代码和宽表格是允许局部横向滚动的例外。

验证宽度为 320、360、390、768、1024、1440px，另检查 767/768 与 1023/1024 断点边界。每个断点两种主题都要覆盖；菜单另用 1024×400 的低高度视口检查。

## 15. Motion、焦点与可访问性

- 120ms 用于按钮按下、Hover 色彩与阴影；180ms 用于主题相关颜色过渡。禁止 transition: all；仅指定必要属性。
- 点击动作立即发生，不等动画完成。导航、菜单和正文不位移；按钮最多移动 2px，装饰最多旋转 1°。
- 不做角色动画、页面转场、打字机、骨架闪光、视差或自动漂浮。
- prefers-reduced-motion 下非必要 transition 时长为 0，取消旋转、位移和平滑滚动；反馈仍通过颜色、线条和文字表达。
- 标准 focus-visible 为 3px focus 色外轮廓，offset 4px；周围保留空间，不能因 overflow 或贴边而裁切。
- 提供跳到主要内容链接；显示时使用 note 背景和 ink 轮廓，层级为 panel。隐藏菜单和隐藏按钮不可进入 Tab 顺序。
- 常规操作目标至少 44×44px；桌面菜单项按项目约定至少 40px 高。正文内文字链接不强行扩为方形目标，以行高和间隔保障阅读。
- 当前页面、当前分类、复制状态使用文字或语义状态与视觉共同表达，不仅靠颜色。
- 普通内容图片有适当替代文本，装饰图和装饰 SVG 对辅助技术隐藏；不禁用缩放、选择或复制。
- 200% 缩放时保持内容顺序与完整导航；禁用 JavaScript 后仍可通过原规划的静态索引读文章，不以全页透明度或隐藏样式等待脚本。

## 16. Design Tokens 完整索引

颜色和字体 Token 的完整值见 §4、§5；这两节与本节共同构成唯一 Token 注册表。实现时使用 CSS 自定义属性，取值含 var() 的项为有意的语义别名。断点用于媒体查询常量，不把 CSS 自定义属性直接写入媒体查询条件。

### 16.1 基础、布局与排版补充

| Token | 默认 / 变化 | 用途及限制 |
|---|---|---|
| `--space-1` | `4px` | 最小间距 |
| `--space-2` | `8px` | 图标、标签间距 |
| `--space-3` | `12px` | 控件与元信息间距 |
| `--space-4` | `16px` | 小面板 padding |
| `--space-6` | `24px` | 段落和组件间距 |
| `--space-8` | `32px` | 中区块间距 |
| `--space-12` | `48px` | 大区块与标题前距 |
| `--space-16` | `64px` | Desktop 大区块间距 |
| `--width-site` | `1120px` | 公共内容最大宽度 |
| `--width-reading` | `740px` | Post 标题与正文最大宽度 |
| `--width-search` | `560px` | 搜索框最大宽度 |
| `--width-status-page` | `640px` | 404 内容最大宽度 |
| `--gutter` | Mobile `16px`；Tablet `24px`；Desktop `32px` | 视口两侧留白 |
| `--section-gap` | Mobile/Tablet `48px`；Desktop `64px` | 首页大区块间距 |
| `--hero-gap` | Mobile/Tablet `24px`；Desktop `48px` | Hero 列或行间距 |
| `--entry-date-width` | Tablet `88px`；Desktop `112px` | Mobile 不使用日期列 |
| `--hero-art-height` | Mobile `220px`；Tablet `300px`；Desktop `360px` | 图片最大高度，不含框 |
| `--about-art-height` | Mobile `220px`；Tablet `240px`；Desktop `300px` | 图片最大高度，不含框 |
| `--status-art-height` | Mobile `180px`；Tablet/Desktop `240px` | 404 图片最大高度 |
| `--avatar-size` | Mobile `64px`；Tablet/Desktop `80px` | Empty State 图尺寸 |
| `--weight-regular` | `400` | 正文、说明 |
| `--weight-medium` | `600` | 控件文字 |
| `--weight-bold` | `700` | 标题与当前项 |
| `--leading-hero` | `1.2` | Hero |
| `--leading-title` | `1.3` | 页面标题、品牌 |
| `--leading-h2` | `1.4` | 二级标题 |
| `--leading-heading` | `1.5` | 三级标题、条目标题 |
| `--leading-meta` | `1.6` | 元信息 |
| `--leading-ui` | `1.65` | 界面文字 |
| `--leading-code` | `1.7` | 代码块 |
| `--leading-body` | `1.85` | 文章正文 |

### 16.2 轮廓、状态、装饰与动效

| Token | 值 | 用途及禁止用途 |
|---|---|---|
| `--line-thin` | `1px` | 内容分隔，不承担唯一焦点 |
| `--line-strong` | `2px` | 交互轮廓、分镜、阅读线 |
| `--radius-none` | `0px` | 主布局、插画图框 |
| `--radius-small` | `2px` | 标签、行内代码 |
| `--radius-control` | `4px` | 按钮、输入框、菜单、提示 |
| `--shadow-none` | `none` | 大多数内容 |
| `--shadow-control` | `3px 3px 0 0 var(--color-shadow)` | 次按钮 |
| `--shadow-emphasis` | `4px 4px 0 0 var(--color-shadow)` | 主 CTA、菜单、复制面板 |
| `--shadow-pressed` | `2px 2px 0 0 var(--color-shadow)` | 按钮按下 |
| `--focus-width` | `3px` | focus-visible |
| `--focus-offset` | `4px` | 外轮廓偏移 |
| `--control-min-height` | `44px` | 普通操作目标 |
| `--icon-size` | `20px` | 功能图标显示尺寸 |
| `--menu-width` | `224px` | 菜单 border-box 宽度 |
| `--menu-item-min-height` | `40px` | 桌面菜单行 |
| `--menu-edge-gap` | `8px` | 视口边距，另计阴影占用 |
| `--menu-padding` | `8px` | 内容、焦点和边框之间的安全空间 |
| `--notice-width` | `360px` | 短提示最大宽度 |
| `--copy-panel-width` | `420px` | 手动复制面板最大宽度 |
| `--float-edge-gap` | `16px` | 浮动控件、提示边距，另计安全区 |
| `--tape-width` | `48px` | 仅局部胶带 |
| `--tape-height` | `16px` | 仅局部胶带 |
| `--decoration-angle` | `-1deg` | 仅胶带；减少动态效果时 0deg |
| `--dot-area-size` | `64px` | 网点区域最大边长 |
| `--dot-size` | `1px` | 网点直径 |
| `--dot-gap` | `8px` | 网点重复间隔 |
| `--dot-opacity` | `0.08` | 仅装饰网点，不用于文字和人物 |
| `--motion-fast` | `120ms` | Hover/Pressed；减少动态效果时 0ms |
| `--motion-normal` | `180ms` | 颜色过渡；减少动态效果时 0ms |
| `--motion-ease` | `ease-out` | 短反馈 |
| `--press-shift` | `1px` | 次按钮；减少动态效果时 0px |
| `--press-shift-strong` | `2px` | 主 CTA；减少动态效果时 0px |

### 16.3 层级与非 Token 常量

| Token / 常量 | 值 | 说明 |
|---|---|---|
| `--z-content` | `0` | 普通内容；避免所有子元素独立建立高层级 |
| `--z-floating` | `30` | 返回顶部、短提示 |
| `--z-progress` | `40` | 阅读进度 |
| `--z-menu` | `100` | 自定义菜单 |
| `--z-panel` | `110` | 复制面板、获得焦点的跳转链接 |
| Tablet 媒体查询常量 | `768px` | 非 CSS 自定义属性 |
| Desktop 媒体查询常量 | `1024px` | 非 CSS 自定义属性 |
| 菜单输入条件 | `(hover: hover) and (pointer: fine)` | 不由断点替代 |

不得为单页引入略有不同的蓝色、阴影或圆角。如果新增视觉状态，先扩展语义 Token 或复用现有状态规则，再实现组件。

## 17. 实现映射、风险与验收

### 17.1 后续实现映射

| 子系统 | 后续位置 | 应用范围 |
|---|---|---|
| 基础系统 | css/base.css | Token、字体、主题、重置、焦点、减少动态效果 |
| 公共组件 | css/components.css | 导航、按钮、标签、条目、菜单、提示、复制面板 |
| 页面布局 | css/pages.css | 四类页面、404 预案及所属响应式规则 |
| 图片 | assets/images/ | §3 两种派生图；不发布原参考图 |
| 验收记录 | docs/acceptance.md | 实际实现后的检查结果、视口、截图和未验证项 |

不新增 JavaScript 公共 API 或文章字段。经典脚本、window.Sywen 命名空间、共享主题与复制动作继续遵循项目规划。HTML 中保留 Header/Footer；资源路径相对站点根解析，文章页使用正确的上级相对路径，不写死域名根路径。

后续实施顺序：准备两种角色派生图 → 基础 Token 与排版 → 公共组件 → Home/Blog/Post/About → 交互状态与菜单 → 响应式与主题验证。404 在 P0 完成后另行执行。本文交付只完成规范，不执行这些网页阶段。

### 17.2 风险与降级

| 风险 | 约束 / 处理 | 验证证据 |
|---|---|---|
| 角色参考细节不一致 | 仅 A01 派生；保留身份特征，不新画姿势 | 原图与裁切并排检查 |
| 原图 1.5–2.1MB，超资源预算 | 不直接发布原图；上身图≤100KB、头像≤30KB；首页资源目标≤500KB | 导出文件字节数、首页冷缓存网络记录 |
| 压缩导致细线丢失 | 实际尺寸看红眼镜、发丝、格纹；优先提高质量而不是锐化重画 | 1×/高像素密度截图 |
| 深色白纸面太抢眼 | 只保留受尺寸约束的原图纸面；周边为深色 surface | 深色 Home/About 截图 |
| 装饰干扰或遮挡 | 局部装饰预算、正文零装饰、禁用指针事件 | 全页截图与键盘焦点检查 |
| 系统字体导致换行差异 | 不固定标题高度；按内容重排 | Windows 与可用手机浏览器 |
| 菜单低高度或长中文超界 | 测量实际尺寸、内部滚动、预留阴影和焦点空间 | 四角、1024×400、200% 缩放 |
| 图片不可用 | 预留比例、短文本回退、主内容先行 | 模拟图片失败 |
| 参考图扩大功能范围 | 单列文章，菜单单层；不增加收藏等 P1 功能 | 对照项目规划范围 |

500KB 沿用原规划的首页资源目标，验收时记录冷缓存实际传输量并同时列出本地资源字节；不把参考目录或本页规范算作首页加载资源。预算不达标需先优化图片与冗余资源，不通过删掉必要文字、导航或焦点反馈达标。

### 17.3 文档交付检查

下列项目用于判断视觉规范是否足够进入前端实现，不代表网页已经通过验收：

- [x] 五张素材有实际尺寸、体积、优先级及上线结论。
- [x] 角色来源、裁切窗口、派生路径、显示上限、替代文本与失败策略已定义。
- [x] 明确图片区分 CSS/SVG，背景图整图不上线。
- [x] 浅深颜色、字体、布局、线条、阴影、圆角、装饰、动效与层级已定义。
- [x] Home、Blog、Post、About、菜单、404 和 Empty State 有视觉及响应式规则。
- [x] 主状态、Hover、Pressed、Focus、选中及复制异常有明确表达。
- [x] 旧规划差异已解决，404/P1 与 P0 边界明确。
- [x] Token 引用在本文内定义，媒体查询常量与 CSS 变量区分。
- [x] 每项实施风险有处理策略，浏览器验收项目有具体场景。

编制验证记录（2026-09-15）：已检查 17 个主章节、93 个 Token 定义；无重复定义、无悬空 var() 引用，文档现有本地素材链接均可解析。按 sRGB 相对亮度计算，ink/muted 分别与 paper、surface、accent、note 的 16 组配色均达到普通文字 4.5:1；浅色最低约 4.87:1，深色最低约 4.51:1。深色 muted 在 note 上余量较小，因此提示文字按 §4 使用 ink，不额外降低透明度。该计算只验证指定实色组合，不替代浏览器实际渲染、覆盖层与图片区域检查。

### 17.4 后续页面验收清单（全部待实现后验证）

| ID | 场景 | 通过标准 |
|---|---|---|
| V01 | Home 首屏、两种主题 | 主标题与 CTA 清楚；一角色＋最多两小装饰；无卡片墙 |
| V02 | 四类核心页面 | 区块顺序与本文一致，角色位置和数量正确 |
| V03 | Blog 默认、组合筛选、无结果、重置 | 布局稳定、无装饰抢占操作；选中和结果状态明确 |
| V04 | Post 段落、三级标题、列表、引用、代码、图片、表格 | ≤740px 阅读列；无背景装饰；宽内容局部滚动 |
| V05 | 320、360、390、768、1024、1440px；断点两侧 | 页面无横向溢出，Header 完整，人物不拉伸，CTA 不被推入图片内部 |
| V06 | 浅深主题及所有控件状态 | 无遗漏主题的控件；普通文字≥4.5:1，大字/关键非文本控件≥3:1 |
| V07 | 200% 缩放、长中文标题、长网址 | 不遮挡、不截断必要内容，焦点可见 |
| V08 | 菜单四角、重复右键、低高度视口 | 单实例，包含阴影的外观不被裁切，聚焦项可见，菜单内可滚动 |
| V09 | 菜单键盘与原生豁免 | ↑/↓、Home/End、Enter/Space、Tab、Esc 正常；链接、编辑与选区保留原生菜单 |
| V10 | 复制成功、失败、file:// | 成功提示可感知；失败面板可手动复制和关闭；本地路径不被分享 |
| V11 | 减少动态效果、纯键盘、触摸设备 | 无非必要运动；焦点完整；触摸不被自定义长按拦截 |
| V12 | 图片加载失败或关闭装饰 | 导航、层级、阅读及重置操作完整可用，无布局大幅跳动 |
| V13 | 资源与部署路径 | 首页预算达标；本地托管；HTTP 子目录及 HTTPS 文章深链接资源正常 |
| V14 | Chrome、Edge、Firefox 与真实手机 | 核心视觉及交互可用；没有真实设备则明确记录未验证 |
| V15 | 禁用 JavaScript | 静态导航、正文、文章入口可用，不残留不可操作的增强控件 |
| V16 | 404（仅 P1 实现后） | 视觉一致，恢复入口可用，另验证托管平台真实 404 行为 |

实现者在 docs/acceptance.md 记录浏览器、日期、视口、主题、结果及截图。未执行项写“未验证”，失败项写复现方式与修复结果。只有 P0 场景通过、无阻塞阅读或导航问题，才视为网页视觉验收完成；一次自动扫描不能替代人工检查。
