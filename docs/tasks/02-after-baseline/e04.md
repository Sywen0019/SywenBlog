# E04 — 复制与快捷菜单

- 状态：**Passed（本地，2026-09-16）**
- 依赖：B06 Passed；可用时间足以完成本任务及回归。E06 另依赖实际领取的增强任务。
- 执行者：工程（DeepSeek，本会话）。
- 输入：已冻结基线、对应参考与接口；豆包按外部任务包交接，不假设本会话可调用。
- 输出：T13/T14；复制降级→完整菜单。
- 预算：4–6h。
- 验收：新增效果实际看图；不破坏原基线内容、键盘、主题、路径和功能；同视口对照。
- 下一任务：可停止或领取另一任务；发布增强版前执行 E06。
- 实际记录：**已领取并本地完成**。`js/context-menu.js`、`js/site.js` 共享动作与 `reading.js` 的 `requestTop`、七页复制面板标记与脚本接入、`css/components.css` 第 8 节；证据见 [docs/evidence/e04/](../../evidence/e04/)（三浏览器各 19 项通过、39 张截图），验收结论见 [验收记录](../../acceptance.md)「E04」章节，接口与新契约见 [交接记录](../../agent-handoffs.md)「E04」。

## 领取与完成情况

1. **范围**：§15.1–§15.6（启用范围、菜单内容、视觉与层级、定位与关闭、键盘、复制与降级）＋ §16.1／§16.3／§16.4（主题文案同步、菜单进度复用、统一状态区）＋ §20.2／§20.3（共享动作与加载顺序）。
2. **共享动作**：`Sywen.notify`／`hideNotice`（统一状态提示）、`Sywen.copyText`（Clipboard API，失败转 `false`）、`Sywen.siteUrl`／`publicUrl`、`Sywen.focusSearch`、`Sywen.goTop`（转调 `reading.js` 的 `requestTop`）。菜单不维护第二套主题、搜索、进度或返回顶部实现。
3. **降级顺序**：先做复制降级（面板 + 共享动作），再补完整菜单；两次提交，可分别回退。
4. **验证**：`node docs/evidence/e04/menu-checks.mjs --browser=edge,chrome,firefox` → 各 19 项通过、0 失败；`node scripts/check-baseline.mjs` → 158 项通过、0 失败；`node docs/evidence/e03/reading-checks.mjs` → 各 18 项通过（脚本按新契约最小更新）。
5. **未执行**：推送、部署、VC2 —— 按用户决定留给 E06。
6. **已知限制**：未使用实体手机与屏幕阅读器；混合输入设备按主指针判定不显示菜单按钮，未在真实混合设备验证；观感判定留给 E06 的 VC2。
