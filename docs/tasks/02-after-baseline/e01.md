# E01 — 文章小画体系

- 状态：Passed（本地，2026-09-16）
- 依赖：B06 Passed；可用时间足以完成本任务及回归。E06 另依赖实际领取的增强任务。
- 执行者：豆包（Seedream 5.0 Pro 出图 + 工程融合与验收）。
- 输入：已冻结基线、对应参考与接口；豆包按外部任务包交接，不假设本会话可调用。
- 输出：学业（study）、生活（life）、我喜欢的（favorites）三张无人物分类小画，融合到首页分类入口卡与 Blog 文章条目缩略图；首页「最近文章」保持纯文字。
- 边界：学业暂不细分，不新增子分类插画；“我喜欢的”可为空，首页分类入口可用对应小画，不为条目演示新增文章。
- 预算：原估算 3–4h，领取时按三类范围复核，不作为当前剩余工时。
- 验收：新增效果实际看图；不破坏原基线内容、键盘、主题、路径和功能；同视口对照。
- 下一任务：可停止或领取另一任务；发布增强版前执行 E06。
- 实际记录：本地完成，未推送 Gitee、未部署 Cloudflare、不宣布 VC2 通过（发布仍归 E06）。
  - 三张 4:3 母版（2364×1773，存于 gitignored 的 `art-work/categories/`，不发布）：study 终稿 candidate-3（2–3 本叠放的书＋书签＋合上的笔记本电脑，物件居中、电脑完整入画）；life 终稿 candidate-1（摊开的手账本＋斜搭的笔＋冒热气的咖啡）；favorites 终稿 candidate-2（单盆居中向日葵盆栽）。study/life 纯墨线＋暖纸、不上红，仅极淡蓝点缀；favorites 向日葵克制上色。favorites 首版含红色入耳耳机与杨枝甘露，用户判定「违和感太强」后移除；study candidate-2 因右侧电脑被裁切否决。
  - 确定性导出脚本 `scripts/export-category-art.py`：Lanczos 缩放到 640×480、WebP q88 method 6、不写元数据，校验 4:3 与单文件 ≤40KB，并生成 `docs/category-assets.json`（尺寸/字节/SHA256/来源母版）；三张 WebP 为 18406/18234/29072 字节。
  - 融合：分类数据新增 `image` 字段；`Sywen.createPostEntry(post, headingLevel, options)` 新增第三参，仅 Blog 动态列表传 `{thumbnail:true}`，Blog 无脚本静态列表补 4 个缩略图；首页分类入口改为上图下文卡片（手机横卡），首页「最近文章」不传缩略、保持纯文字。缩略图为装饰性、`alt=""`、`aria-hidden`、`loading="lazy"`、`decoding="async"`，加载失败时图框收起、文字链接完整。
  - 证据 `docs/evidence/e01/illustration-checks.mjs`：Edge/Chrome/Firefox 各 49 项通过、0 失败（资产格式/尺寸/字节/sha、动态与静态缩略映射、桌面/手机尺寸、装饰图不可聚焦且 alt 空、懒加载、失败收起、无脚本、子目录、浅深色、320–1440px 溢出矩阵）。全量 `scripts/check-baseline.mjs` 161/0；E03 阅读增强回归三浏览器各 18/0、E04 复制与菜单回归各 19/0（两处证据脚本的像素/时序按 E01 后博客列表做了最小兼容更新，见验收记录）。
