# 视觉检查点记录（VC1 / VC2）

本文件记录 Plan.md 规定的两个集中视觉检查点：**VC1**（Stage 4 完成门槛：七页静态视觉）与 **VC2**（Stage 8 完成门槛：交互状态与响应式最终视觉）。模型分工为 Astra Review + DeepSeek Revision；本文件只记录问题与复核结论，不记录源码改动细节（源码改动见 `Change_log.md` 与 Git 历史）。

当前状态：**未执行**。Stage 0～1 只建立文档基线与语义骨架，没有可评审的视觉产物；T04（Stage 2）已完成样式基础并留下 `docs/evidence/t04/` 的两主题截图与像素级测量，但那属于工程自检，**不构成 VC1 结论**。任何视觉结论在本文件出现前都视为未验证。

## T04 自检与 VC1 的关系（非检查点结论）

- T04 的自检结论与偏差修复记录在 [验收记录 §阶段 2 · T04](acceptance.md) 与 [渲染报告](evidence/t04/render-report.txt)。
- 自检由执行任务的 DeepSeek 会话完成，未调用指定的视觉 Review 模型；且该会话模型不支持图片输入，截图结论来自像素直方图、包围盒与行列剖面测量，观感类判据（是否像卡片墙、文字是否拥挤、角色姿态是否符合 A01）未确认。
- VC1 仍须在 T07／T08 完成后执行，输入为两主题、多视口的正式截图包，并重新截图（不得复用 T04 证据）。

## 问题记录格式（Plan.md §5.2）

```text
Issue ID：VC1-01
页面／组件：
视口／主题／状态：
证据：
实际表现：
规范依据：
允许修改范围：
具体修复要求：
验收方式：
是否阻塞：
是否需要 Astra 局部复核：
```

只有违反既定规范、破坏可读性或妨碍既定页面效果的问题进入修订任务；纯个人审美偏好不进入本轮实施。

## VC1 — 七页静态视觉 Review

- 状态：未执行（等待 T05～T08 完成并自检通过）。
- 输入要求：视觉上下文包；Home、Blog、编程 Post、About 两主题截图；其余三篇完整页面截图；角色对照图（`参考素材/角色三视图.png` 与派生图并排）。
- Review 范围：四类页面整体，另外三篇文章的长标题、密度与模板一致性。
- 重点：留白、比例、层级、垂直节奏、角色使用、深色纸面、条目密度、正文阅读感；Desktop／Tablet／Mobile 的既定重排。
- 允许建议：纠正偏离 `DESIGN_SPEC.md` 的尺寸、间距、对齐、状态和装饰使用，采用规范已有值。
- 禁止建议：重选配色／字体、改变角色、添加封面墙／侧栏／新功能、改变页面顺序或部署方式。
- 问题清单：待写入。
- 门槛结论：待写入。

## VC2 — 交互状态与响应式最终视觉 Review

- 状态：未执行（等待 T17 候选版）。
- Review 范围：四类页面代表性视口；Blog 选中／无结果；主题动作；Focus／Pressed；菜单四角和低高度；复制面板／提示；返回顶部及进度线。
- 重点：交互加入后是否破坏留白、层级、阅读列、角色比例、焦点可见性；两主题与窄屏是否符合既定规则。
- 禁止建议：重新打开 VC1 已确定且未受影响的风格决策；新增功能、装饰、布局体系。
- 问题清单：待写入。
- 门槛结论：待写入。

## 局部复核记录

两个检查点各预留一次必要的局部复核；无问题时不再调用。复核记录（范围、输入版本、结论、是否仍阻塞）在此追加。
