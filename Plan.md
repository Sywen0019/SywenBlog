# 实施计划：先完成作业基线，再按余量完善

修订日期：2026-09-16。B00–B06 已完成，达到作业完成线；E01–E06保持Deferred。取代旧 T05–T22 全量首发门槛。历史 T00/T01/T02/T04 保留，不重做、不改写历史。交付与证据见 [docs/delivery.md](docs/delivery.md)。

## 作业完成线

七页内容完整、CSS 生效、JavaScript 主题与搜索分类可演示、Git 分阶段提交、Gitee 源码可检查、公开 HTTPS 首页与文章深链接可访问，并且书桌 Hero 奠定漫画美术基调。只有 B06 与全部基线阻塞项关闭，才算作业完成。

## 顺序与预算

|任务|产物|依赖|工时|
|---|---|---|---|
|B00|要求映射、Gitee/托管通路和最小发布验证|现有仓库|1h|
|B01|七页内容、摘要、日期、标签、相邻链接|B00 本地核对|3–4h|
|B02|书桌 Hero 候选及 VC0|B01 内容密度|3–4h|
|B03|全站基线视觉融合|B01、B02|3–4h|
|B04|主题、搜索与分类组合、必要降级|B03|3–4h|
|B05|VC1-B 内容/视觉/功能/响应式验收|B04|2–3h|
|B06|Gitee 推送、正式 HTTPS、版本与演示记录|B05、发布通路已验证|1–2h|

另预留 2h 缓冲，总剩余 18–24h。外部发布障碍不阻止独立本地工作；出图等待时可推进正文和 JS。默认单执行者，不预支并行收益。B01–B05 各里程碑提交和推送，B06 不是首次提交。

**B00 → B01 → B02/VC0 → B03 → B04 → B05 → B06 ══ 作业完成，可停止提交 ══ E 类按余量领取。**

## 功能与接口

- 七页、三个 CSS、经典脚本与闭包；只通过 window.Sywen 共享功能。相对路径与 data-site-root 保留。
- 主题 sywen.theme：合法保存值 → 系统 → 浅色；存储失败可切换；未主动选择时跟随系统变化。
- 搜索标题、摘要、标签，忽略拉丁大小写、按空白拆词 AND，与分类共同生效。保持 q/category/focus=search；未知分类回全部；history.replaceState 失败不影响筛选；中文组合输入完成后更新。
- 初始化失败保留静态索引；结果数量延迟约 300ms 礼貌播报，不夺取焦点。
- 文章字段保留 id/slug/title/summary/date/category/tags/readingTime/url/isDemo；同日 id 升序。四篇明确标示示例，约 600–900 字，编程篇可至约 1200 字。日期为实际首次完成日期。
- DOM 新增先登记 docs/agent-handoffs.md。未实现增强控件保持 hidden。

## 美术基线

全站 70% minimal + 30% manga，Hero 可达 60/40。桌面约等宽文图两列、4:3 插画、440–520px 显示宽度；手机文案/CTA 后展示人物。三视图锁定短发、呆毛、红框眼镜、格纹身份。书桌以笔记本电脑为主要设备，书写使用平板与手写笔，人物注视笔尖所在区域，不边写边看镜头。只强依赖一张新书桌 Hero；About 可复用已有裁切；正文最大 740px、零背景装饰。

Astra 在 Codex 调用内置出图完成复杂场景、人物和重绘。豆包外部执行者用其内置出图做无人物分类小画、植物、贴纸；这些全放基线后。Astra 集中统一验收。工程可做确定性裁切压缩，不将重绘静默转给其他模型。

Hero 先 2 个构图候选，选 1 个、最多 2 次针对性修订；仅两图都不可用再做第 3 个。预计 3–5 次调用，不强制用满。母版与候选不发布，记录提示词/参考/工具/版本/选择理由。

## 基线之后（默认 Deferred）

|任务|内容|工时|
|---|---|---|
|E01|豆包四类小画、Home/Blog 接入、Astra 一致性检查|3–4h|
|E02|Astra About 阅读角色、豆包植物贴纸与转场|3–5h|
|E03|返回顶部与阅读进度，焦点/减少动态效果/重算|1.5–2.5h|
|E04|先复制降级，再单层右键菜单完整键盘/定位/原生豁免|4–6h|
|E05|状态小角色、移动裁切与少量精修|2–3h|
|E06|仅已完成增强的 VC2、回归与新版发布|1.5–2.5h|

每次只领取一包，不自动清空任务。先考虑 6–12h 高收益增强；36–48h 仅为完整增强参考，不是必须投入。404、季节主题、逐篇封面、更多人物留 Backlog。新版本保留基线可回退。

## 验收与发布

VC0：1440/390 浅深 Hero 实际看图。VC1-B：七页内容、角色对照、核心 JS、无 JS/初始化失败/存储失败、键盘、320/390/768/1440 与断点、200% 缩放、图片失败、子目录。Edge/Chrome 主流程、Firefox 核心；真实手机未运行明确记录。

Hero 单个选用 WebP ≤300KB，首页冷缓存初始 ≤800KB、完整滚动 ≤1.2MB 为目标；Hero 不懒加载，图片固有尺寸与稳定占位。超目标先优化裁切、编码、请求尺寸，再记录实际影响。

Gitee 为源码提交入口；正式发布通路为 Cloudflare Pages 连接 GitHub 仓库的 `main` 分支（构建命令 `bash scripts/build-site.sh`，输出目录 `dist`，见 [部署说明](docs/deployment.md)）；Netlify 与 GitHub Pages 保留为历史备选。仅发布 index.html/blog.html/about.html/posts/css/js/assets 和部署说明性元文件，不发布参考图、候选、文档、凭据。网站本体无构建依赖，`dist/` 仅为发布白名单拷贝，不做 SPA 重写。部署 commit、Gitee commit 和公开核验分别记录，不用推送成功代替部署成功。

任务卡见 docs/tasks/00-index.md；旧编号映射、实际状态与证据以任务卡、交接、验收为准。
