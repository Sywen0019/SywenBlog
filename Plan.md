# Sywen’s Space 前端实施与 Agent 调度计划

日期：2026-09-15  
状态：计划已落盘；尚未开始前端实施。  
本文包含已批准的 Implementation Baseline 与其任务拆分、模型调度细化。落盘不代表任何开发阶段已完成。

## 文档导航

- [第一部分：Implementation Baseline](#第一部分implementation-baseline)
- [第二部分：任务拆分与模型调度](#第二部分任务拆分与模型调度)

# 第一部分：Implementation Baseline

## 一、实施基线与交付范围

### 已检查的 Workspace

- 已完整阅读 [PROJECT_PLAN.html](PROJECT_PLAN.html) 的 29 章、[DESIGN_SPEC.md](DESIGN_SPEC.md) 的 17 章及 [AGENTS.md](AGENTS.md)。
- 已逐张检查角色三视图、背景参考图、网页参考图和两张辅助参考图。
- 功能规划实际文件为 `PROJECT_PLAN.html`，不存在 `PROJECT_PLAN.md`；不另建内容重复的功能规划。
- 基线调查时没有应用代码、构建配置、测试框架或 Git 仓库。本次仅新增本文档。

### 固定决策

采用 **HTML5 + CSS3 + Vanilla JavaScript 多页面静态网站**，无生产依赖、打包器、前端框架、后端或 CMS。

功能范围以项目计划为准；视觉实现以设计规范为准。设计规范已经覆盖旧计划中的书桌插画要求，主视觉使用现有角色裁切。

首发交付：

- Home、Blog、About 和四篇独立 Post，共七个页面。
- 搜索、分类及组合筛选、主题切换、返回顶部、阅读进度。
- 桌面自定义右键菜单、复制链接及失败降级。
- 响应式、基本可访问性、验收记录、Gitee 仓库和公开 HTTPS 网站。

404、目录、收藏及其他 P1/P2 功能不进入本轮实施。

## 二、页面、样式与资产架构

### 1. HTML 页面结构

所有页面使用 `lang="zh-CN"`、独立文档标题和描述、一个 `h1`，包含跳到主要内容链接及语义化 Header、Nav、Main、Footer。Header/Footer 直接保留在七份 HTML 中，通过检查清单同步维护。

| 页面 | 内容顺序与实现要求 |
|---|---|
| Home | Header → Hero → 最近三篇文章 → 四类入口及数量 → 最近在做 → Footer |
| Blog | 标题说明 → 搜索 → 分类 → 结果数量与重置 → 单列文章／无结果状态 |
| Post | 返回列表 → 分类、标题、日期、阅读时间 → 示例标识 → 正文 → 标签 → 相邻文章 |
| About | 关于我 → 学习方向 → 兴趣 → 最近在做 → 三组静态 FAQ |

补充约束：

- 顶层页面使用 `index.html`、`blog.html`、`about.html`；文章位于 `posts/`。
- Home 最近文章和 Blog 四篇简版索引直接写入 HTML，JavaScript 失效时仍可阅读和导航。
- Blog 筛选工具默认隐藏，动态列表成功初始化后再显示并替换静态索引；初始化失败保留静态内容。
- Header 正常文档流；手机保留完整三项导航，不增加汉堡菜单。
- Post 中“上一篇”指更旧文章，“下一篇”指更新文章；边界省略不存在的链接。
- 标签链接进入 Blog 搜索；分类链接进入对应分类。
- 未实现的增强控件不提前显示成可操作按钮。

### 2. CSS 架构与 Design Tokens

按顺序加载三个样式文件：

| 文件 | 职责 |
|---|---|
| `css/base.css` | 基础重置、完整 Tokens、字体、主题、基础排版、焦点、辅助技术工具类、减少动态效果 |
| `css/components.css` | 导航、按钮、分类、标签、文章条目、搜索、旁白、状态提示、阅读控件、菜单、复制面板 |
| `css/pages.css` | Home / Blog / Post / About 布局及页面特有响应式规则 |

实施要求：

- 按设计规范 §4、§5、§16 原样落实完整 Token 注册表，包括颜色、字体、间距、尺寸、边框、阴影、动效和层级。
- 使用语义颜色变量；浅深主题仅切换变量映射，不对页面或角色使用反色滤镜。
- 公共内容最大宽度 1120px，Post 头部与正文最大宽度 740px。
- 保留 1px 分隔线、2px 交互轮廓、3/4px 硬阴影、0/2/4px 圆角的既定分工。
- 正文字号手机 17px、平板及桌面 18px，行高 1.85；标题使用规范中的流式字号。
- 移动优先，媒体查询使用固定的 768px、1024px；响应式规则紧邻所属组件或页面。
- 两空格缩进、kebab-case 类名；不增加通用组件框架或大量工具类。

### 3. 公共组件

复用样式和必要的小型 DOM 创建函数，不通过 JavaScript 注入公共导航。

组件包括：

- 品牌刊头、当前导航、主题按钮、快捷菜单按钮、Footer。
- 主次 CTA、分类按钮、标签、日期＋内容文章条目。
- 搜索输入、结果工具行、Empty State。
- 角色图框、最近在做旁白面板。
- 正文标题、引用、代码、表格、图片与图注、相邻文章导航。
- 返回顶部、阅读进度、统一状态提示、手动复制面板、自定义菜单。

每个交互组件一次性实现默认、Hover、Pressed、Focus、选中及适用的异常状态。普通导航保持链接语义；菜单专用 ARIA 角色仅用于自定义菜单。

### 4. 视觉资产接入

原始五张 PNG 留在参考目录，不覆盖、不移动，也不作为网页资源加载。

| 发布资产 | 派生方式 | 使用位置 |
|---|---|---|
| `assets/images/character-front-upper.webp` | 角色原图裁切 `(120, 0, 320, 600)`，输出 320×600 | Home、About |
| `assets/images/character-front-avatar.webp` | 裁切 `(175, 0, 225, 225)`，输出 225×225 | Blog 无结果状态 |
| `assets/images/search-flow.svg` | 简单原创搜索流程示意图 | 编程示例文章 |
| `assets/icons/favicon.svg` | 简单文字或线性标识 | 全站 |

资产规则：

- 两种角色 WebP 初始质量 90，目标分别不超过 100KB、30KB。
- 采用确定性裁切和编码，不生成新角色，不抠图、重绘、镜像、拉伸或改变配色。
- 核验呆毛、红眼镜、双臂轮廓及格纹；如裁切边缘确需微调，仅向空白区调整并同步设计规范。
- 保留固有宽高。Hero 不懒加载；其他图片依据位置加载，隐藏状态图避免不必要的首屏请求。
- Home 图高上限按 Mobile / Tablet / Desktop 为 220 / 300 / 360px；About 为 220 / 240 / 300px。
- 深色保留角色自身白纸面，周围继续使用深色主题。
- Hero 和 Empty State 角色使用空替代文本；About 使用规范指定的角色描述。
- 图片失败保留空间，图框显示“Sywen”；空状态头像可隐藏。
- 背景使用 CSS 纸色；胶带、网点使用 CSS。普通图标采用统一 24×24 视框、2px 描边的内联 SVG。
- Home 最多一处角色和两处小装饰，About 最多一片胶带；Mobile 隐藏小装饰，Post 正文零背景装饰。

## 三、JavaScript 模块与功能契约

使用经典脚本和闭包，只通过 `window.Sywen` 共享必要数据与动作。除头部提前执行的主题脚本外，其余使用 `defer`，不使用 ES Modules、`fetch` 数据加载或事件总线。

### 1. 模块职责

| 模块 | 职责与共享内容 |
|---|---|
| `js/theme.js` | 提前设置主题，读取／切换主题，同步按钮文字，处理系统主题及存储异常 |
| `js/posts-data.js` | 提供 `posts` 与 `categories`，沿用既定字段，不增加新文章字段 |
| `js/site.js` | 站点相对路径解析、首页列表与分类数量、公共搜索入口、复制链接、统一提示 |
| `js/blog.js` | 搜索分类状态、URL 同步、结果渲染和结果播报 |
| `js/reading.js` | 返回顶部、Post 页面滚动进度，提供共享当前进度 |
| `js/context-menu.js` | 条件启用、单实例菜单、定位、关闭、键盘和焦点管理，调用公共动作 |

建议最小共享函数采用 `resolveUrl`、`getTheme`、`toggleTheme`、`openSearch`、`backToTop`、`copyPageLink`、`notify`、`getReadingProgress`；模块内部辅助函数保持私有。

加载顺序：主题 → 数据 → 公共逻辑 → 对应页面功能 → 阅读工具 → 菜单。只初始化当前页存在的功能。

顶层 HTML 标记站点根为 `./`，Post 为 `../`；数据里的路径相对站点根解析。禁止域名根路径和硬编码部署域名。

### 2. 文章数据与内容

沿用字段：

`id`、`slug`、`title`、`summary`、`date`、`category`、`tags`、`readingTime`、`url`、`isDemo`。

固定四篇：

| 分类 | 标题 | 文件名 |
|---|---|---|
| 人工智能 | 用直觉理解注意力机制 | `attention-intuition.html` |
| 编程 | 原生 JavaScript 搜索与筛选笔记 | `dom-search-notes.html` |
| 科研 | 一次论文阅读如何留下可复用笔记 | `paper-reading-notes.html` |
| 生活 | 给学习留一点空白 | `leave-some-space.html` |

- 每篇约 600～900 字，编程篇可至约 1200 字。
- 使用明确的“示例文章”标识；不编造经历、实验结果或引用。
- 日期使用实际首次完成日期，不伪造历史发布日期；同日按 `id` 升序稳定排列。
- 摘要约 50～90 字，每篇 2～4 个标签；阅读时间按约 400 中文字／分钟估算后人工复核。
- 编程篇覆盖代码、引用、流程图与图注，并加入简单表格验证正文样式。
- 静态索引、首页前三篇、分类数量、文章头部和相邻链接必须一致。

### 3. 功能实现顺序与行为

#### ① 主题切换

- 存储键 `sywen.theme`，只接受 `light`、`dark`。
- 初始化顺序：合法保存值 → 系统主题 → 浅色。
- 头部在样式绘制前尽早设置根元素主题；只有主动切换才写存储。
- 未主动选择时跟随系统变化；读写存储失败不影响当前页切换。
- Header 与菜单调用同一逻辑，动作名称同步。
- JavaScript 禁用时保持可读主题，隐藏不可用的主题按钮。

#### ② 搜索与分类

- 即时匹配标题、摘要、标签；正文不参与。
- 去首尾空格、忽略拉丁字母大小写、按空白拆词；多个词采用 AND。
- 分类与关键词共同生效；切分类保留关键词，清空关键词保留分类。
- 重置清空关键词并恢复全部；未知分类回退全部。
- 初始化读取 `q`、`category`，更新使用 `history.replaceState`；失败仅停止 URL 同步。
- 中文输入法组合输入期间不破坏输入过程；组合完成后更新最终结果。
- 使用 DOM API 和 `textContent`，不将用户输入拼入 HTML。
- 视觉结果即时更新，数量延迟约 300ms 礼貌播报，不移动搜索焦点。
- `focus=search` 仅在请求时聚焦搜索框；跨页搜索入口使用此参数。

#### ③ 返回顶部与阅读进度

- 滚动超过 480px 显示返回顶部；安全区外保留至少 16px，点击目标至少 44×44px。
- 返回后聚焦主标题，避免聚焦再次改变预期滚动位置。
- 减少动态效果时即时滚动，否则平滑滚动。
- Post 进度为 `scrollTop / (scrollHeight - clientHeight)`，限制到 0～100%；分母不大于零时为 100%。
- 使用文档滚动元素和 `requestAnimationFrame` 合并更新；Resize、图片加载后重算。
- 2px 顶部进度线提供 `progressbar` 名称与数值，不持续播报。
- 菜单百分比直接复用此值。

#### ④ 公共复制与反馈

- 普通页面复制当前 URL；Post 复制去掉查询参数和片段的文章 URL。
- Clipboard 成功显示“链接已复制”，约 4 秒后消失，不移动焦点。
- API 不可用或拒绝时显示非模态手动复制面板：可见标签、只读地址、选中文本、关闭按钮。
- Esc／关闭恢复合理焦点；不增加遮罩或强制焦点陷阱。
- `file://` 不复制或展示可供分享的本地磁盘路径，显示规范中的公开网站提示。

#### ⑤ 自定义右键菜单

- 仅在 `(hover: hover) and (pointer: fine)` 下启用，不按屏幕宽度判断。
- 保留编辑控件、可编辑区域、链接、媒体、非空选区、Shift 右键及显式豁免区域的原生菜单；检查目标及祖先。
- 不拦截触摸长按；仅确定可以显示自定义菜单后调用 `preventDefault()`。
- 单层固定菜单：导航 → 搜索／返回顶部／主题 → 复制；Post 增加独立只读进度。
- 挂载到 `body`，使用 `fixed`；实际测量后限制位置，保留 8px 边距并额外容纳 4px 阴影。
- 低高度内部滚动，聚焦项滚入可视区；内部滚动不关闭菜单。
- 可见“快捷菜单”按钮支持 Enter/Space；打开聚焦首项，方向键循环，Home/End 到首尾。
- Esc 恢复打开前焦点；Tab/Shift+Tab 关闭并进入相对打开前焦点的正常前后顺序。
- 外部点击不抢焦点；搜索和返回顶部以动作目标为最终焦点。
- 操作、外部点击、页面滚动、Resize、窗口失焦、输入能力变化时关闭；重复右键只更新同一实例。
- 所有功能均保留普通页面入口，菜单只承担桌面捷径。

## 四、分阶段实施与 Git Milestones

沿用 **18 小时实施＋2 小时缓冲**作为目标估算；验收失败优先修复，不通过削减 P0 边界行为赶工。每阶段更新验收记录，通过后提交，每日推送。

| 阶段 | 工作内容 | 阶段验收条件 | Commit |
|---|---|---|---|
| 0 · 基线与准备／1h | 初始化 `main`；建立 README、忽略规则和验收记录；修正 AGENTS 中素材数量等过时事实；检查 Gitee、托管账户和网络条件 | 文档依据明确；原图保留；仓库无凭据；远端推送可用或明确记录外部阻塞 | `docs: define implementation baseline and delivery checklist` |
| 1 · 七页结构／1h | 建立页面、公共导航、相对链接和静态文章入口；使用最小版本试部署 | 七页可直接访问；文章深链接与资源可用；禁用 JS 仍可导航 | `feat: add semantic pages and global navigation` |
| 2 · 资产与视觉基础／2h | 导出两种角色图片；建立 Tokens、字体、两套主题和基础组件 | 裁切无身份变化；图片尺寸与预算合格；组件状态、焦点和主题变量齐备 | `style: establish design tokens and character assets` |
| 3 · Home → Blog／2h | 完成首页视觉、静态列表、分类入口、Blog 正常与空状态样式；登记四篇元数据 | 首页仅前三篇；单列文章；文案和 CTA 完整；390px、1440px 两主题符合规范 | `feat: build home and blog page layouts` |
| 4 · Post → About／2h | 完成四篇正文、流程图、相邻导航及 About 内容 | 七页静态视觉完成；正文≤740px；正文元素齐全；无虚构履历、空链接或装饰干扰 | `feat: add sample posts and about page` |
| 5 · 核心交互／3h | 按主题 → 筛选 → 返回顶部／进度 → 复制反馈顺序实现 | 组合查询、URL、持久化、异常存储、滚动边界及复制降级通过 | `feat: add filtering theme and reading interactions` |
| 6 · 菜单增强／2h | 接入公共动作、定位、原生豁免、键盘和焦点行为 | 四角、低高度、重复开启、关闭条件、Tab/Esc、触摸豁免全部通过 | `feat: add accessible desktop context menu` |
| 7 · 响应式与可访问性／2h | 全站断点、缩放、对比度、键盘与减少动态效果检查及修复 | 指定宽度无整体横向溢出；两主题均可读；核心流程可纯键盘完成 | `fix: improve responsive layouts and accessibility` |
| 8 · 完整验收／2h | 跨浏览器、资源、异常降级、元数据一致性及性能检查 | P0 无阻塞缺陷；检查有证据；未验证项明确；首页目标≤500KB | `test: record acceptance results and visual verification` |
| 9 · 正式交付／1h | 推送、正式部署、记录双网址与版本、整理截图及演示说明 | 干净副本可复现；公开仓库与 HTTPS 页面可访问；版本可追溯、可回退 | `docs: finalize deployment and release instructions` |

阶段 4 是静态结构与视觉门槛，通过后才进入复杂 JavaScript。响应式和可访问性从阶段 1～2 开始落实，阶段 7 负责系统复核。

## 五、验收与发布

### 1. 响应式和可访问性验收

| 项目 | 验收范围 |
|---|---|
| 视口 | 320、360、390、768、1024、1440px；另测 767/768、1023/1024 边界 |
| 低高度 | 菜单和复制面板检查 1024×400，以及 200% 缩放 |
| Mobile | 两行 Header、Hero 文案先行、日期上移、分类换行、装饰隐藏 |
| Tablet | Hero 双栏，About 单栏；正文与控件不拥挤 |
| Desktop | Hero 3:2；日期窄列；正文保持阅读宽度 |
| 可访问性 | 跳转链接、可见标签、标题层级、正确按钮／链接语义、完整焦点顺序 |
| 对比度 | 普通文字≥4.5:1；大字及关键非文本控件≥3:1；检查实际背景组合 |
| 动效 | 减少动态效果时移除非必要过渡、位移、旋转和平滑滚动 |

不使用全局 `overflow-x: hidden` 掩盖问题；只有代码和宽表格允许局部横向滚动。隐藏控件不得进入 Tab 顺序。

### 2. 功能与降级测试

依据项目计划 §26 和设计规范 V01～V15，至少覆盖：

- 搜索字段、空白、大小写、多词 AND、中文输入、组合分类、未知分类、无结果与重置。
- URL 直达、刷新、标签跳转、搜索聚焦入口、历史更新异常。
- 主题合法／非法保存值、无保存值、系统变化、存储读取和写入失败。
- 滚动顶部／中部／底部、短页、图片加载、调整窗口、返回顶部焦点。
- 菜单原生豁免、各类关闭条件、完整键盘行为和输入能力变化。
- 剪贴板成功、不可用、拒绝、手动复制和本地文件提示。
- 图片加载失败、长标题、长网址、长代码和宽表格。
- 禁用 JS、数据脚本加载失败、`file://`、HTTP 根目录、HTTP 子目录、正式 HTTPS。
- 七页导航、文章元数据、分类数量、相邻关系、控制台异常和资源 404。

Chrome、Edge 完整检查，Firefox 检查核心流程，至少一个真实手机浏览器检查导航、搜索、正文和主题。没有设备或浏览器条件的项目写“未验证”，不得用模拟视口冒充真实设备结果。

不引入测试框架；使用浏览器开发者工具、人工操作和必要的临时检查脚本。验收记录包含日期、浏览器、视口、主题、步骤、预期、实际、截图及修复结果。

首页记录冷缓存传输量和本地资源字节，目标不超过 500KB；不加载远程字体或运行时 CDN。

### 3. Gitee 与静态部署

**源码：Gitee `origin`，单人 `main` 小步提交。**

- 阶段 0 检查现有登录和仓库条件；真实账号、远端地址和访问权限在执行时获取，不填写虚构地址。
- 账号或授权缺失只阻塞相应远端操作，本地实施继续。
- README 记录本地 HTTP 启动方式、目录、文章维护步骤、公共 HTML 同步清单、素材来源和演示流程。
- 素材来源沿用“用户提供的工作区参考素材”，不补写未知作者或授权结论。

**首选：Netlify 手动部署。**

- 阶段 1 先验证最小页面、文章深链接和课程网络访问。
- 从已提交版本准备发布目录，仅包含页面、CSS、JS、发布资产；排除原始参考图、规划文档、凭据及临时文件。
- 不设置构建命令，不添加 SPA 重写。Netlify 官方说明手动部署不会运行构建命令，适合本项目。[部署说明](https://docs.netlify.com/deploy/create-deploys/)

**备选：Gitee 主仓库＋GitHub 镜像＋GitHub Pages。**

- 仅在首选账户设置或目标网络验收失败时启用。
- 使用分支发布静态页面，保持项目子目录兼容；Gitee 继续保存完整开发历史。[GitHub Pages 建站说明](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)
- 不把 Gitee Pages 可用性作为交付前提，不承诺托管平台价格或额度。

**正式版本验收：**

1. 完成 P0 检查，提交最终代码与文档。
2. 将 `v1.0.0` 标记到正式源码提交，并从该提交准备发布文件。
3. 检查公开首页、Blog、About、四篇文章深链接、主题、搜索、复制及资源加载。
4. 记录仓库地址、网站地址、部署时间、发布提交与验收结果；后补的部署记录明确指向发布提交。
5. 从干净副本复核本地启动与发布文件一致性。
6. 出现发布问题时重新部署上一可用提交；交付前和演示前执行公开网址冒烟检查，不新增监控服务。

最终交付判定：**七页内容完整、视觉符合既有规范、P0 功能及降级可用、验收证据真实、Gitee 与公开网站均可追溯到明确版本。**

# 第二部分：任务拆分与模型调度

## 1. 基线、范围与调度原则

### 1.1 本版与原实施计划的关系

本文第一部分作为 **Implementation Baseline，简称 B**，继续完整有效。第二部分只细化执行任务、模型分工、依赖及交接，不替换其中的功能契约、视觉规则或验收标准。

- 功能依据 [PROJECT_PLAN.html](PROJECT_PLAN.html)，简称 **P**。
- 视觉依据 [DESIGN_SPEC.md](DESIGN_SPEC.md)，简称 **D**。
- 仓库规则 [AGENTS.md](AGENTS.md)。
- 五张参考 PNG 已存在；本次仅落盘 `Plan.md`，不执行前端实现、Git 操作或资产修改。
- 后续维护在本文中保留 B 全文及调度细化，避免形成两份互相漂移的实施基线。

### 1.2 保持不变的实施边界

| 项目 | 已批准基线 |
|---|---|
| 技术 | HTML5、CSS3、Vanilla JavaScript；经典脚本、无构建依赖 |
| 页面 | Home、Blog、四篇 Post、About，共七页 |
| 视觉 | D 的 Tokens、版式、角色裁切、装饰预算和响应式规范 |
| 内容 | 四篇指定示例文章，静态 HTML 正文、JavaScript 元数据 |
| 功能 | 搜索及组合分类、主题、返回顶部、阅读进度、复制降级、桌面右键菜单 |
| 降级 | 静态导航及文章索引、存储异常、图片失败、剪贴板失败、本地文件限制 |
| 发布 | Gitee 主仓库＋Netlify 手动部署；既定 GitHub Pages 备选 |
| 排除 | P1/P2、框架、后端、CMS、额外设计方向 |
| 阶段 | 保留 B 的 Stage 0～9、目标、验收及 commit milestones |

本版的小任务验收是原验收标准的细化。任何任务“通过”均不豁免 B、P §26 或 D §17.4。

### 1.3 模型使用规则

- **DeepSeek**：默认工程执行模型，负责实现、机械检查、修复、测试、文档和 Git。
- **Astra**：只负责两个集中的视觉检查点；默认不改源码。
- 标准闭环：**DeepSeek Implementation → Astra Visual Review → DeepSeek Revision**。
- 不按任务重要程度分配 Astra，不为常规工程问题升级模型。
- 模型名称表示任务执行要求；执行环境若无法调用 DeepSeek，应记录调度阻塞，不静默替换为 Astra。
- 两个检查点均预留一次必要的局部复核；无问题时不调用复核。不得为每个页面或修复单独启动 Astra。
- 原 18 小时实施＋2 小时缓冲仍为目标估算，不为每个子任务另加预算，也不以预算为由降低验收标准。

## 2. 最小上下文与交接协议

### 2.1 上下文包

每个 Agent 先读取仓库规则、当前任务卡和直接依赖任务的交接，再读取表中指定章节与相关源码。无需重复读取整个项目历史或全部参考图片。

| 包 | 内容 | 适用任务 |
|---|---|---|
| C0 基线 | B 的范围、阶段、相关契约；本调度计划；AGENTS | 所有任务 |
| C1 结构与内容 | P §9～14、§19～21；B 页面与数据契约 | HTML、内容、路径 |
| C2 视觉基础 | D §3～10、§12.1、§14～16 | 资产、Tokens、公共样式 |
| C3 页面视觉 | D §11 对应页面、§14～15、§17.4 对应项 | 页面样式 |
| C4 核心交互 | P §16、§19～20；D §12 对应组件；B 功能契约 | 主题、列表、阅读、复制 |
| C5 菜单 | P §15；D §12.2～12.3、§16.3；公共动作接口 | 右键菜单 |
| C6 验收发布 | P §22～26；D §17.2、§17.4；B 发布规则 | 验收、Git、部署 |

“读取上下文包”只指本任务涉及的条目；例如主题任务不需要阅读文章内容规则。

视觉 Review 额外读取：

- D §1 已解决的视觉差异。
- 对应页面的相关章节、当前截图、发布资产。
- 必要时查看对应源码。
- 角色原图用于身份与裁切核对；网页参考图用于关系核对，不按截图逐像素复刻。
- 两张辅助参考仅在既定规则仍无法解释具体视觉问题时查看。

### 2.2 统一交接记录 H

每个任务必须留下：

1. Task ID、模型、状态：`Ready / Running / Blocked / Review / Passed`。
2. 基础版本：commit；尚未初始化 Git 时记录输入文件快照信息。
3. 实际修改文件、修改摘要。
4. 新增或使用的 DOM 约定、函数签名、参数、返回值、加载顺序。
5. 自检步骤、实际结果、截图或其他证据。
6. 未验证项、已知问题、是否影响后续任务。
7. 下一任务 ID，以及接手者必须注意的事项。

任务正文中的“交接”是在 H 之外需要特别记录的内容。

执行后记录放在 `docs/`：

- `agent-handoffs.md`：任务交接索引与记录。
- `acceptance.md`：沿用原计划的正式验收记录。
- `visual-review.md`：视觉问题、修复与复核记录。

这些仅为执行记录，不增加产品功能或运行时依赖。

### 2.3 单一记录与集成负责人

由 DeepSeek 承担集成职责，归入 T00、T11、T17、T21、T22，不另设常驻协调 Agent。

- 并行任务只返回交接内容，不同时编辑共享记录。
- 集成负责人统一写入记录、核对差异并执行 commit。
- 只有达到原 milestone 的任务集合才提交对应 milestone；不要求每个子任务单独提交。
- 不伪造提交过程、不压缩成最后一次总提交。

## 3. 子任务目录

> 下列尚未存在的文件均为预期输出，不代表本轮已创建。路径均相对项目根目录。写入范围之外的问题必须记录并交回，不自行扩大修改范围。

### Stage 0～2：基线、骨架和视觉基础

#### T00 — 执行基线与工程准备

**模型：DeepSeek｜原阶段：Stage 0｜依赖：本计划获批并获准开始实施**

- **目标与输入**：依据 C0、C6，核对本文保留的 B 全文和调度规则，完成原 Stage 0 工程准备。
- **允许修改**：`Plan.md`、`README.md`、`.gitignore`、`AGENTS.md` 中过时事实、`docs/`；执行原计划授权范围内的 Git 初始化和远端准备。
- **输出与验收**：基线可追溯；原始素材保留；忽略规则有效；真实远端配置可用，或明确记录缺失账号／权限。不得更改原计划功能。
- **交接**：H＋文档索引、账户检查结果、Git 状态、共享记录负责人。
- **下游**：T01、T02。

#### T01 — 七页语义骨架与路径契约

**模型：DeepSeek｜原阶段：Stage 1｜依赖：T00 本地准备通过**

- **目标与输入**：依据 C1、B 的七页结构、文章文件名和相对路径约定，建立可独立访问的页面骨架。
- **允许修改**：三个顶层 HTML、四个 Post HTML。
- **输出**：公共 Header/Footer、七页区域结构、静态文章入口、站点根标记、页面标识、增强控件的隐藏挂载位置。
- **验收**：七页链接互通、直接打开可读、唯一 h1、跳转链接有效；禁用 JS 不影响导航；文章页相对路径正确。
- **交接**：H＋DOM ID、`data-*`、脚本挂载位置和公共 HTML 同步清单。后续任务沿用这些约定。
- **下游**：T03、T04。

#### T02 — 指定视觉资产派生

**模型：DeepSeek｜原阶段：Stage 2 的资产准备｜依赖：T00**

- **目标与输入**：依据 D §3、§9、角色三视图、B 资产契约，完成确定性资产派生。
- **允许修改**：两种角色 WebP、`favicon.svg`；仅发生允许的裁切微调时同步 D 的裁切坐标。
- **输出**：既定尺寸的上身图、头像及 favicon，不产生新姿态或新视觉方向。
- **验收**：尺寸、文件格式、体积目标符合基线；无拉伸、镜像、重绘和相邻人物；保留身份细节；原图不变。
- **交接**：H＋最终坐标、尺寸、编码参数、字节数、原图与派生图对照证据。不能解决的视觉疑问交 VC1。
- **下游**：T04。

#### T03 — 最小版本部署验证

**模型：DeepSeek｜原阶段：Stage 1｜依赖：T01、T00 远端条件具备**

- **目标与输入**：依据 C6、七页骨架和原部署方案，提前验证托管及网络条件。
- **允许修改**：README、验收和交接记录；执行既定最小部署。源码问题退回 T01。
- **输出**：最小部署地址和对应提交；课程网络检查结果。
- **验收**：公开 HTTPS、首页和文章深链接可访问；保持无构建部署；记录真实平台与网络结果。
- **交接**：H＋部署地址、版本、平台设置、访问证据或外部阻塞。
- **下游**：T22；可与后续本地工程并行，不阻塞 T04。

#### T04 — Tokens、公共组件与响应式基础

**模型：DeepSeek｜原阶段：Stage 2｜依赖：T01、T02**

- **目标与输入**：依据 C2、七页骨架、派生资产，实现统一视觉基础。
- **允许修改**：三个 CSS 文件；七页 HTML 的样式挂钩、资源引用和公共组件标记。
- **输出**：完整 Tokens、浅深主题变量、字体、容器、导航、按钮、条目、旁白、焦点和基础响应式规则。
- **验收**：Tokens 对应 D；无悬空变量；公共组件状态齐备；手机导航完整；静态切换根主题属性可检查两种主题；首页局部已呈现既定风格。
- **交接**：H＋组件类名、CSS 分区、主题属性、已验证宽度与状态。此任务是共享 CSS 的首次集中所有者。
- **下游**：T05。

### Stage 3～4：页面完成与第一个视觉检查点

#### T05 — Home 与 Blog 静态视觉实现

**模型：DeepSeek｜原阶段：Stage 3｜依赖：T04**

- **目标与输入**：依据 C1、D §11.1～11.2、相关组件规则及 B 四篇元数据契约，完成两个发现文章的页面。
- **允许修改**：Home、Blog HTML、`posts-data.js`、三个 CSS 文件的必要部分。
- **输出**：首页各区块、前三篇、真实分类数量、Blog 静态索引、筛选及空状态样式、四篇元数据。
- **验收**：内容顺序、日期列、角色尺寸、装饰预算正确；两主题在 390px／1440px 可完整查看；无封面卡片墙或占位 CTA。
- **交接**：H＋冻结的四篇元数据、排序、文章头部字段表；空状态的临时预览方法，预览状态不得残留生产页面。
- **下游**：T06。

#### T06 — 首篇 Post 模板与正文样式

**模型：DeepSeek｜原阶段：Stage 4｜依赖：T05**

- **目标与输入**：依据 C1、D §11.3、正文排版规则和冻结元数据，建立可复用文章模板。
- **允许修改**：编程文章 HTML、三个 CSS 文件中正文相关部分、`search-flow.svg`。
- **输出**：完整编程示例文章及可复用 Post 模板，覆盖代码、引用、图注、表格和相邻导航。
- **验收**：正文宽度、字号、局部滚动符合规范；正文无装饰；示例标识明确；元数据一致。
- **交接**：H＋文章模板、正文元素示例、相邻链接表、其他三篇填充规则。
- **下游**：T07、T08。

#### T07 — 其余三篇文章实现

**模型：DeepSeek｜原阶段：Stage 4｜依赖：T06**

- **目标与输入**：依据 C1 的内容规则、T06 模板、T05 元数据完成三篇文章；只读对应正文样式。
- **允许修改**：人工智能、科研、生活三个 Post HTML。
- **输出**：三篇完整示例正文及对应标签、日期、阅读时间和相邻链接。
- **验收**：达到 B 的内容要求；头部与数据一致；不虚构经历或引用；无空链接；既定模板保持一致。
- **交接**：H＋各文章内容长度、元数据核对、链接检查结果。必要的数据修正请求交集成负责人，不跨范围修改。
- **下游**：VC1。

#### T08 — About 页面实现

**模型：DeepSeek｜原阶段：Stage 4｜依赖：T06**

- **目标与输入**：依据 D §11.4、角色和响应式规则、P §13 及现有公共组件，完成 About。
- **允许修改**：About HTML、`pages.css` 中 About 专属区段。
- **输出**：介绍、角色、学习方向、兴趣、最近在做、三组静态 FAQ。
- **验收**：桌面双栏、Tablet/Mobile 单栏；角色尺寸正确；无虚构履历、空社交链接或折叠交互。
- **交接**：H＋两主题和三类布局证据、与公共组件复用情况。
- **下游**：VC1。

T07 与 T08 可并行：修改文件不重叠；公共 CSS 问题留给后续集中修订。

#### VC1 — 七页静态视觉 Review

**模型：Astra｜原阶段：Stage 4 完成门槛｜依赖：T07、T08；T05～T08 自检通过**

- **目标与输入**：视觉上下文包；Home、Blog、编程 Post、About 两主题截图；其余三篇完整页面截图；角色对照图。确认静态视觉与 D 一致。
- **允许修改**：仅视觉 Review 记录，不改源码、资产或规范。
- **Review 范围**：四类页面整体，另外三篇文章的长标题、密度与模板一致性。
- **重点**：留白、比例、层级、垂直节奏、角色使用、深色纸面、条目密度、正文阅读感；检查 Desktop／Tablet／Mobile 的既定重排。
- **允许建议**：纠正偏离 D 的尺寸、间距、对齐、状态和装饰使用；采用 D 已有值及规则。
- **禁止建议**：重选配色／字体、改变角色、添加封面墙／侧栏／新功能、改变页面顺序或部署方式。
- **输出与验收**：按问题格式输出可执行清单，或明确通过；必须引用规范及页面位置，不以“更好看”为唯一理由。
- **交接**：H＋阻塞问题、非阻塞问题、证据、修复边界、哪些修复需要局部复核。
- **下游**：T09。

#### T09 — 静态视觉修订与门槛关闭

**模型：DeepSeek｜原阶段：Stage 4｜依赖：VC1**

- **目标与输入**：依据 VC1 清单及对应源码完成修订；只读涉及的 D 章节。
- **允许修改**：Review 明确点名的 HTML/CSS；仅已批准裁切修正涉及的派生资产。
- **输出**：逐项修复和同视口对照证据；无问题时仅整理门槛记录。
- **验收**：全部阻塞问题关闭；机械修正按数值和截图验证。VC1 指定需要审美复核的修复，提交同一个 Astra 检查点集中复核一次。
- **交接**：H＋问题→修改→验证映射，明确静态视觉门槛是否通过。
- **下游**：T10；未通过不得开始 Stage 5。

### Stage 5～6：核心 JavaScript 与菜单

#### T10 — 公共路径、列表复用与页面动作

**模型：DeepSeek｜原阶段：Stage 5｜依赖：T09**

- **目标与输入**：依据 C4、T01 DOM 契约、T05 数据、B 共享接口，稳定后续功能依赖。
- **允许修改**：`site.js`；Home 和 Blog 的必要挂载标记。
- **输出**：路径解析、首页数据渲染、分类数量、搜索入口和公共提示基础；冻结共享接口及脚本依赖。
- **验收**：根目录／子目录路径正确；数据加载失败时静态入口保留；渲染不注入不可信 HTML；无重复全局命名空间。
- **交接**：H＋共享函数完整签名、DOM 依赖、成功／失败行为、加载顺序。
- **下游**：T11、T12。

#### T11 — 主题与滚动阅读工具

**模型：DeepSeek｜原阶段：Stage 5｜依赖：T10**

- **目标与输入**：依据 P §16.1～16.3、B 主题／返回顶部／进度契约和 T10 接口实现主题与阅读功能。
- **允许修改**：`theme.js`、`reading.js`；集中同步七页脚本引用和阅读／主题控件标记。
- **输出**：主题初始化及切换、系统跟随、存储降级、返回顶部、共享阅读进度。
- **验收**：主题优先级、失败存储、按钮同步、480px 阈值、焦点、短页 100%、Resize／图片加载重算、减少动态效果全部符合 B。
- **交接**：H＋主题状态规则、进度读取接口、公共动作调用方式、已验证异常。
- **下游**：T13。

#### T12 — Blog 搜索、分类与 URL 状态

**模型：DeepSeek｜原阶段：Stage 5｜依赖：T10**

- **目标与输入**：依据 P §11、§16.4、§19、B 搜索契约、Blog DOM 和数据文件，实现列表增强。
- **允许修改**：仅 `blog.js`。需要 HTML 调整时交给 T11 的集成负责人处理。
- **输出**：组合筛选、初始化查询、URL 更新、中文输入处理、结果数量播报、空状态和重置。
- **验收**：多词 AND、大小写、空白、分类保留、未知分类、URL 异常、焦点和延迟播报通过；初始化失败保留静态索引。
- **交接**：H＋查询状态规则、测试输入与预期、任何 DOM 调整请求。
- **下游**：T13。

T11 与 T12 可并行：T12 不改 HTML、共享样式或公共脚本。

#### T13 — 复制降级与核心交互集成

**模型：DeepSeek｜原阶段：Stage 5｜依赖：T11、T12**

- **目标与输入**：依据 B 复制契约、D §12.3、T10～T12 接口和交接，完成复制及 Stage 5 集成。
- **允许修改**：`site.js`、`components.css` 对应提示／面板区段；必要 HTML 和前三项功能的集成修复。
- **输出**：复制成功提示、非模态手动复制面板、本地文件提示；完整 Stage 5 集成版本。
- **验收**：成功／拒绝／不可用／file 场景通过；面板焦点及关闭正确；主题、搜索、阅读组合使用无冲突。
- **交接**：H＋稳定的公共动作契约、失败恢复焦点规则、Stage 5 验收结果。
- **下游**：T14。

#### T14 — 桌面菜单实现与行为自检

**模型：DeepSeek｜原阶段：Stage 6｜依赖：T13**

- **目标与输入**：依据 C5、已稳定公共动作和菜单挂载约定，实现桌面增强菜单。
- **允许修改**：`context-menu.js`、菜单对应 CSS、七页快捷菜单按钮的必要标记。
- **输出**：单实例菜单、能力判断、原生豁免、定位、内部滚动、键盘和焦点处理。
- **验收**：完整执行 P §26.3；菜单不重复实现主题、复制或滚动功能；隐藏菜单不进入 Tab 顺序。
- **交接**：H＋事件／关闭条件清单、原生豁免测试、四角和低高度截图、未解决问题。
- **下游**：T15、T16。

### Stage 7～8：系统验收与第二个视觉检查点

#### T15 — 响应式与可访问性审计

**模型：DeepSeek｜原阶段：Stage 7｜依赖：T14**

- **目标与输入**：依据 D §14～15、P §18、§26，审计四类页面和交互。
- **允许修改**：不改源码；返回任务结果和证据，由集成负责人入档。
- **输出**：全部指定宽度、断点边界、200% 缩放、键盘、焦点、对比度、减少动态效果的检查清单。
- **验收**：每个场景有实际结果；发现问题给出最小复现、定位和对应规范；不擅自提出视觉方向变化。
- **交接**：H＋机械可修复项、需要 Astra 判断项、未验证项。
- **下游**：T17。

#### T16 — 功能、内容和降级审计

**模型：DeepSeek｜原阶段：Stage 8 的前置检查｜依赖：T14**

- **目标与输入**：依据 P §26、B 全部功能验收条目、相关 JS 和元数据，检查功能及降级完整性。
- **允许修改**：不改源码；返回测试报告和证据。
- **输出**：搜索、主题、滚动、复制、菜单、静态降级、元数据、链接、资源预算及部署路径检查结果。
- **验收**：Chrome／Edge 完整检查、Firefox 核心检查、真实手机可用性按条件记录；未运行不标通过；失败可复现。
- **交接**：H＋浏览器矩阵、异常测试证据、资源字节与冷缓存传输量、缺陷列表。
- **下游**：T17。

T15 与 T16 只读同一冻结版本，可由两个 DeepSeek Agent 并行。测试过程不能互相改变浏览器状态；使用独立会话或顺序执行有冲突的场景。

#### T17 — 集中修复与视觉候选版整理

**模型：DeepSeek｜原阶段：Stage 7～8｜依赖：T15、T16**

- **目标与输入**：依据两份审计结果及涉及源码，集中关闭工程缺陷。
- **允许修改**：缺陷涉及的 HTML、CSS、JS、元数据和正式记录；不改变功能契约。
- **输出**：集中修复版本、回归结果、VC2 截图包。
- **验收**：工程缺陷关闭；修复后的核心流程重新验证；涉及视觉不确定性的问题明确交 VC2。
- **交接**：H＋候选版本、截图清单、修复映射、仍需视觉判断的具体问题。
- **下游**：VC2。

#### VC2 — 交互状态与响应式最终视觉 Review

**模型：Astra｜原阶段：Stage 7～8 完成门槛｜依赖：T17**

- **目标与输入**：依据 VC1 已批准记录、T17 候选版、相关 D 章节和截图，确认最终视觉一致性；不重读全部文章。
- **允许修改**：仅视觉 Review 记录。
- **Review 范围**：四类页面代表性视口；Blog 选中／无结果；主题动作；Focus／Pressed；菜单四角和低高度；复制面板／提示；返回顶部及进度线。
- **重点**：交互加入后是否破坏留白、层级、阅读列、角色比例、焦点可见性；两主题和窄屏是否符合既定规则。
- **允许建议**：修复规范偏差与响应式视觉回归。
- **禁止建议**：重新打开 VC1 已确定且未受影响的风格决策；新增功能、装饰、布局体系。
- **输出与验收**：给出规范驱动的可执行问题清单或通过结论；机械正确性不重复交给 Astra 测试。
- **交接**：H＋需修复项、修复后需要局部复核的项、最终视觉门槛状态。
- **下游**：T18。

#### T18 — 最终视觉修订与局部回归

**模型：DeepSeek｜原阶段：Stage 8｜依赖：VC2**

- **目标与输入**：依据 VC2 问题列表、涉及源码和规范，完成最终视觉修订。
- **允许修改**：Review 指定范围及修复必须涉及的关联位置。
- **输出**：逐项修复和前后对照；无问题时直接整理记录。
- **验收**：阻塞问题关闭；受影响功能回归通过。需审美复核的项集中返回 VC2 一次，不为每项新开 Review。
- **交接**：H＋最终视觉结论、修复版本、回归范围。
- **下游**：T19、T20。

#### T19 — 发布候选版本验收

**模型：DeepSeek｜原阶段：Stage 8｜依赖：T18**

- **目标与输入**：依据最终候选源码、已有测试结果、P §26、D V01～V15，作发布前验收。
- **允许修改**：验收记录，不直接修改产品源码。
- **输出**：P0 完成矩阵、未验证项、最终资源与路径结果。
- **验收**：无阻塞缺陷；最后修复涉及的场景复测；既有有效证据可复用，不无理由重复全部测试。
- **交接**：H＋候选版本通过／失败结论、真实设备覆盖情况、发布限制。
- **下游**：T21。失败时退回对应工程任务，并重新验证受影响范围。

#### T20 — README 与交付文档整理

**模型：DeepSeek｜原阶段：Stage 9 的文档准备｜依赖：T18**

- **目标与输入**：依据 B 文档要求、实际目录、交接和既有部署信息，整理交付说明。
- **允许修改**：README、部署及演示说明；不覆盖仍由 T19 更新的验收记录。
- **输出**：运行方法、文章维护流程、公共 HTML 同步规则、素材来源、演示顺序、部署与回退说明。
- **验收**：说明与实际实现一致；无虚构网址、未完成项冒充完成或错误依赖。
- **交接**：H＋需由 T22 补齐的真实版本、网址和部署结果字段。
- **下游**：T21。

T19 与 T20 可并行：代码冻结，文档写入范围分开。

### Stage 9：版本与发布

#### T21 — Git Milestone 汇总与发布版本准备

**模型：DeepSeek｜原阶段：Stage 9｜依赖：T19、T20**

- **目标与输入**：依据 C6、真实提交历史、验收结论和最终文档，准备可追溯发布版本。
- **允许修改**：交付记录；执行基线规定的最终提交、推送和版本标记。
- **输出**：可追溯正式源码版本及从已提交版本准备的发布目录。
- **验收**：milestone 历史真实；工作区状态清楚；发布文件符合基线白名单；不包含参考原图、凭据或临时文件。
- **交接**：H＋commit、tag、发布文件清单、Gitee 状态。
- **下游**：T22。

#### T22 — 正式部署与交付核验

**模型：DeepSeek｜原阶段：Stage 9｜依赖：T21、T03 部署条件已验证**

- **目标与输入**：依据正式发布文件、C6、既定平台配置，完成正式上线核验。
- **允许修改**：部署、README 和验收记录；按原方案发布。
- **输出**：Gitee 地址、公开 HTTPS 地址、部署时间、对应版本、最终冒烟结果。
- **验收**：七页和文章深链接可访问；资源、搜索、主题、复制正常；干净副本可复现；发布与回退版本明确。
- **交接**：H＋最终交付索引、演示入口、剩余非阻塞限制。
- **下游**：本轮 P0 交付完成；不自动启动 P1。

## 4. 执行顺序与并行边界

### 4.1 推荐顺序

```text
T00  DeepSeek
 ├─ T01  DeepSeek
 └─ T02  DeepSeek
       ↓
T04  DeepSeek
  → T05 → T06
             ├─ T07  DeepSeek
             └─ T08  DeepSeek
                       ↓
VC1  Astra
  → T09  DeepSeek
  → 必要时 VC1 集中局部复核
                       ↓
T10  DeepSeek
 ├─ T11  DeepSeek
 └─ T12  DeepSeek
             ↓
T13 → T14  DeepSeek
             ├─ T15  DeepSeek
             └─ T16  DeepSeek
                       ↓
T17  DeepSeek
  → VC2  Astra
  → T18  DeepSeek
  → 必要时 VC2 集中局部复核
                       ↓
 ├─ T19  DeepSeek
 └─ T20  DeepSeek
             ↓
T21 → T22  DeepSeek
```

部署支线：`T01 → T03 → T22`。T03 可与本地工程并行，但正式交付前必须关闭部署阻塞。图中的汇合点等待任务卡列出的全部依赖，不以图形位置推断额外依赖。

### 4.2 串行规则

以下默认串行：

- Tokens／公共组件建立 → 页面实现。
- Home／Blog → Post 模板；先稳定共享正文样式，再批量填充文章。
- 修改 `base.css`、`components.css`、`pages.css` 的共享区段。
- 公共接口实现 → 消费接口的功能。
- 核心动作稳定 → 菜单复用。
- Review → Revision → 规定的复核／门槛关闭。
- 最终验收通过 → 正式版本与发布。

不得让多个 Agent 同时修改公共 CSS、`site.js`、七页公共结构或正式验收文件。

### 4.3 并行默认上限

默认最多两个 DeepSeek 工程任务并行。仅使用上文列出的独立组合，不为了占满模型槽位增加任务。

并行前必须：

1. 冻结共同输入版本。
2. 明确文件所有权。
3. 提供已稳定接口。
4. 指定集成负责人。
5. 合并后检查真实差异并执行必要回归。

共享工作区直接遵守文件所有权；隔离 checkout 也不得借此放宽所有权或跳过集成验收。

## 5. Visual Checkpoint、问题处理与完成门槛

### 5.1 Astra Review 输入标准

DeepSeek 在调用 Astra 前必须提供：

- 可浏览的完整页面或可靠截图，不提交半成品片段要求猜测。
- 页面、视口、主题、交互状态及对应版本。
- 自检结果，已排除资源 404、脚本错误等常规工程问题。
- 与前一次 Review 的变化摘要。
- 需要视觉判断的问题；没有疑问时说明需核对的规范范围。

VC1 重点是静态整体；VC2 重点是交互和响应式回归。两者不重复承担整站工程验收。

### 5.2 视觉问题输出格式

每项采用：

```text
Issue ID：VC1-01
页面／组件：
视口／主题／状态：
证据：
实际表现：
规范依据：
允许修改范围：
具体修复要求：
验收方式：
是否阻塞：
是否需要 Astra 局部复核：
```

只有违反既定规范、破坏可读性或妨碍既定页面效果的问题进入修订任务。纯个人审美偏好不进入本轮实施。

### 5.3 门槛与阻塞处理

- 每个任务先自检；未通过不得标记 `Passed`。
- 阻塞任务只停止依赖它的分支，已独立的分支可以继续。
- DeepSeek 可按 D 的明确数值修复 CSS，不可自行改变视觉方向。
- Astra 可指出规范偏差，不可扩大范围或改写基线。
- 明显矛盾记录为 **Plan Issue**：涉及条款、影响任务、复现证据、为何无法在现有规则内执行。等待确认后处理，不偷偷改方案。
- 缺少账号、权限、真实设备或指定模型能力分别记录为外部阻塞／未验证项，不伪造结果。
- Astra 局部复核仍失败时保留门槛未通过状态，记录剩余问题；不得自动展开无限 Review 循环。

### 5.4 Git Milestone 与原阶段保持一致

T00 对应 Stage 0；T01/T03 对应 Stage 1；T02/T04 对应 Stage 2；T05 对应 Stage 3；T06～T09 与 VC1 对应 Stage 4；T10～T13 对应 Stage 5；T14 对应 Stage 6；T15/T17 对应 Stage 7；T16～T19 与 VC2 对应 Stage 8；T20～T22 对应 Stage 9。

沿用 B 的 milestone commit 名称与阶段验收。跨阶段的提前检查属于准备工作，不提前宣布后续阶段完成；实际修复可追加独立提交。

**最终完成条件不变：七页内容完整、视觉符合既定规范、P0 功能与降级可用、验收证据真实、Gitee 与公开网站可追溯到明确版本。**
